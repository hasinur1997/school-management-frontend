/**
 * Admissions review module (task 2.6) — the authenticated queue + detail with
 * approve/reject. The public, unauthenticated submission/status flow (task 2.5)
 * lives in `../public`.
 */

export { AdmissionsList } from "./admissions-list"
export { AdmissionDetail } from "./admission-detail"
export { ADMISSION_VIEW, ADMISSION_MANAGE } from "./permissions"
