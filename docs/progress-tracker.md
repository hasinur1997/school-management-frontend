# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes. Progress state must reflect the actual implementation, not the intended state.

This frontend consumes the Laravel REST API documented in `docs/api/*.md` and `docs/api-spec.md`. Each feature below has a spec in `feature-specs/`.

## Current Phase

- Phase 4: Exams, Results, Promotion (ready)

## Current Goal

- Task 4.4 (`tasks/task-4.4-result-pdfs.md`): Result-sheet PDF download — blocked on backend 8.4.
- Task 5.1 (`tasks/task-5.1-fee-structures.md`): Fee-structure CRUD implemented (backend 10.1); invoice generation deferred to backend 10.2.
- Task 5.2 (`tasks/task-5.2-invoices.md`): Invoices list/detail/my-invoices implemented (backend 10.2/10.3), now with **multi-line-item invoices** (an invoice's total is the sum of described charges). Next: 5.3 payments.
- Task 5.3 (`tasks/task-5.3-payments.md`): **done**. The **money-receipt paper** (`components/invoices/receipt-paper.tsx`) was already implemented; this task wired the payment actions. `components/invoices/invoice-actions.tsx` (a print-hidden panel under the invoice document, mounted by `InvoiceDetail`) offers **Pay online** (any authorized viewer → `POST /invoices/{id}/payments/online` → redirect to the SSLCommerz gateway URL via the shared `paymentRedirectUrl` helper; return lands on `/invoices/{id}?paid=1`, which `InvoiceDetail` reconciles by **refetching** and stripping the marker — the API/IPN is the source of truth, never the redirect), **Record payment** (staff `fee.collect` → `local-payment-dialog.tsx` → `POST /invoices/{id}/payments/local`), and a per-settled-payment **Receipt** download (`use-download-receipt.ts` streams `GET /payments/{id}/receipt` as a blob via the raw `http` client and saves it with `lib/download.ts`). **Partial payments** are gated by a settings toggle read defensively via `hooks/settings/use-payment-settings.ts` (`useAllowPartialPayment`, `GET /settings`) — since settings task 6.4 isn't done yet, an unreadable/absent toggle defaults to **off**, locking the amount to the full outstanding balance. New hooks: `use-payment-mutations.ts` (`useStartOnlinePayment`/`useRecordLocalPayment`, the latter invalidating `["invoices"]`). Types: `LocalPaymentInput`, `invoiceOutstanding`/`invoiceHasOutstanding` (integer-cents string math, no float). `npm run build` + `tsc` pass; `lint` clean (only pre-existing warnings). **Contract confirmed against backend `~/Herd/app-api`:** `POST /invoices/{id}/payments/local` body is `{ amount }` (`decimal:0,2`); the partial rule lives in `PaymentService::assertAcceptableAmount` keyed on the per-branch `partial_payment_enabled` setting (exposed via `GET /settings`, which requires `setting.manage` — so a collector without it reads the safe `false` default and the UI locks to full payment). Receipt PDF is `GET /payments/{id}/receipt` (paid payments only, streamed by dompdf).
- Task 5.3 **follow-up — collect across all a student's invoices** (student-detail Billing tab): the backend has **no bulk endpoint**, so `components/invoices/collect-payment-dialog.tsx` (staff `fee.collect`, `useStaffPath` only) records a total (or, when `partial_payment_enabled`, a partial of it) by **client-side allocation** oldest-invoice-first (`lib/payment/allocate.ts` `allocateOldestFirst`, integer-cents string math) into one `POST …/payments/local` per invoice, run **sequentially** by `hooks/invoices/use-collect-payment.ts` which stops at the first failure and reports how much was applied (`CollectPaymentResult.failedAt`). A full-total payment fills each invoice to its exact outstanding, satisfying the backend's per-invoice "full payment required" rule even when partial is off; a partial total (partial-on) only splits the boundary invoice. Outstanding invoices are gathered via `hooks/invoices/use-student-outstanding.ts` (`GET /invoices?student_id=&per_page=100`, filtered to `invoiceHasOutstanding` — one page covers a student's monthly invoices). The dialog shows the total, a live oldest-first split preview, and loading/error/empty states; a "Collect payment" button sits beside "New invoice" in `student-invoices-panel.tsx`. Online (gateway) collection stays per-invoice by design (one SSLCommerz session settles one invoice). `tsc`/`lint`/`build` pass.
- Task 5.3 **follow-up — money receipt on the student page**: *(superseded — see the receipt-PDF follow-up below)* the student billing panel briefly rendered `InvoiceActions` + `InvoicePaper` inline on row expand; it is now a compact invoice list matching the main list.
- Task 5.3 **follow-up — receipt as a client-rendered PDF + per-invoice entry points**: the money receipt is now generated **on the frontend** from the on-screen `receipt-paper.tsx` (matched to the imported "Money Receipt" Claude Design), replacing the backend `GET /payments/{id}/receipt` stream (endpoint left intact but unused; the `use-download-receipt.ts` hook and its per-payment download in `InvoiceActions` were removed). A shared exporter `components/invoices/paper-pdf.ts` (html-to-image → jsPDF single content-fit page, reusing the results mark-sheet pattern) backs thin wrappers `receipt-document.ts` / `invoice-document.ts` (`download*`/`print*`). Every invoice now exposes **View invoice** (`/invoices/{id}`) and **View money receipt** (`/invoices/{id}/receipt`) — added to the list rows + mobile cards (`invoices-list.tsx`, actions column now always shown), the student fees panel (`student-invoices-panel.tsx`, reworked from an inline-expand accordion into a compact invoice list matching the main list — each row navigates to the single invoice/receipt pages instead of expanding), and the detail header (`invoice-detail.tsx`, whose Print/Download now render the client PDF). The receipt page (`app/(app)/invoices/[id]/receipt/`, `receipt-view.tsx`) shows **one combined receipt** = the sum of all *settled* payments (`buildCombinedReceipt` in `types/invoice.ts`, integer-cents string math; `methodLabel` names the distinct methods), with a "no receipt yet" empty state for unpaid invoices and Print/Download actions. Gating helper `invoiceHasReceipt` (status ≠ unpaid). `tsc` + `lint` pass (only pre-existing warnings). **Layout fix:** a trailing `<p className="sr-only">` in `receipt-view.tsx` was removed — Tailwind `sr-only` is `position:absolute`, and with no positioned ancestor it anchored to `<html>` at the flow position *below* the tall paper, escaping the app shell's `overflow` clip and adding ~450px of document scroll into blank space (verified via headless-Chrome CDP: the receipt route's `documentElement` scroll dropped from 1064→613, matching the list/detail pages).

- Task 5.3 **follow-up — global payments list (new backend endpoint 10.6)**: added a branch-wide **Payments** browse screen listing every counter (cash) and online payment across invoices — there was no backing API, so a new **`GET /payments`** endpoint was built in `~/Herd/app-api` (staff `invoice.view`, the same gate as the invoice list; branch isolation automatic via `BelongsToBranch`). Backend: `PaymentController::index` + `ListPaymentsRequest` (filters `status`, `method`, `from`/`to` on `paid_at`, `search`, `per_page`) → `PaymentService::list` (eager-loads `invoice.student` **withTrashed** so a soft-deleted student still resolves; `applySearch` spans receipt_no + invoice_no + student name; newest-first) → `PaymentResource` extended so the `invoice` block now also carries `invoice_no`, `month`, `year`, and the billed `student` (`{id, name_en}`) for the list. Route in `routes/api/v1/fees.php`. Verified with `tests/Feature/Api/V1/PaymentIndexTest.php` (7 passing: shape, permission 403, method filter, name search, date range, branch isolation, trashed student). Frontend: `hooks/invoices/use-payments.ts` (`usePayments`, paginated, filters + super-admin `branch` in the key, `keepPreviousData`), `components/invoices/payments-list.tsx` (table ≥ md + mobile cards, status/method selects + inclusive date range + debounced search, each row links to its invoice and — when settled — the money receipt; all four states present), route `app/(app)/payments/` (page gated on `invoice.view` + loading/error), and a **Payments** sidebar item (`nav-items.ts`, `CreditCard`, `invoice.view`). Types in `types/invoice.ts`: `PaymentInvoiceRef`, `Payment.invoice?`, `PaymentListParams`, `PAYMENT_METHODS`/`PAYMENT_STATUSES`, `PaymentMethodFilter`/`PaymentStatusFilter`, `paymentStudentName`. Money via `formatMoney` throughout (no float math). `tsc` + `lint` clean; `npm run build` passes (`/payments` route emitted).
- Task 5.3 **fix — online payment wired to the live sandbox + in-app result popup**: online pay was returning **502** because SSLCommerz credentials were never set — added `SSLCOMMERZ_STORE_ID`/`SSLCOMMERZ_STORE_PASSWORD`/`SSLCOMMERZ_SANDBOX=true` to `~/Herd/app-api/.env` (config-backed via `config/services.php`, read by `SettingsRepository`), verified the exact `SslCommerzGateway::createSession` payload returns `status: SUCCESS` + a `GatewayPageURL` against the sandbox. **Browser return now lands in the app, not raw API JSON**: the `landing` handler (`PaymentController`) 302-redirects to `{config('app.frontend_url')}/invoices/{invoice.public_id}?payment={success|fail|cancel}` (unknown tran → `/invoices?payment=unknown`); new `app.frontend_url` config + `FRONTEND_URL` env (default `http://localhost:3000`); the landing route now `Route::match(['get','post'])` since SSLCommerz **POSTs** the browser to success/fail. Still no DB writes here — the IPN stays the source of truth. **Frontend**: `[id]/page.tsx` parses `?payment=`; `InvoiceDetail` takes `paymentResult`, refetches on return (reusing the existing gateway-return reconcile, now also stripping `payment` from the URL), and renders a new **`components/invoices/payment-result-dialog.tsx`** — a centered popup (built on the DS `Dialog`, with its built-in X close) with success/fail/cancel variants (colored badge via `bg-success|error|muted` tokens, `Done` button). `tsc` passes; landing redirects verified end-to-end via curl (GET + POST + unknown). **Local-dev caveat**: the IPN URL is built from `APP_URL=http://app-api.test`, unreachable by SSLCommerz's servers, so a completed checkout redirects+refetches but the invoice **won't settle** until the IPN arrives — needs a public tunnel (ngrok / Herd Share) on `APP_URL`, or real `APP_URL` in production.

- Task 5.4 (`tasks/task-5.4-finance-income-expenses.md`): **done**. New **Finance** route (`app/(app)/finance/`, gated on `income.manage` OR `expense.manage`) with **Income** and **Expenses** tabs (`components/finance/finance-management.tsx`, `?tab=` persisted; only permitted tabs render). Each tab is a paginated, filterable ledger (`incomes-list.tsx` / `expenses-list.tsx`) — table ≥ md, mobile card list below — with a category + inclusive date-range + debounced search filter bar (`finance-filters.tsx`) and a create/edit dialog (`income-form-dialog.tsx` = `title`; `expense-form-dialog.tsx` = `item_name`; both share amount/date/category/description). **System-generated fee income** (`is_system`) is flagged with a "System" badge and shown read-only — its edit/delete actions are hidden (the API's `403` guard is the real boundary). The reusable **`CategorySelect`** (`category-select.tsx`, type-scoped, backed by `hooks/finance/use-categories.ts` → `GET /categories?type=&per_page=100`) is the dropdown source (also feeds 5.5/5.6); rows carry only the category id hash, so lists map id→name via the cached category list. Money is the API decimal string throughout — `formatMoney`, no float math, no client aggregation. Hooks: `use-incomes.ts`/`use-expenses.ts` (paginated, filters + super-admin `branch` folded into the key, `keepPreviousData`), `use-income-mutations.ts`/`use-expense-mutations.ts` (create/update/delete invalidating `["incomes"]`/`["expenses"]`). Types in `types/finance.ts`. Contract confirmed against backend `~/Herd/app-api` (`IncomeController`/`ExpenseController`/`CategoryController`, `routes/api/v1/accounting.php`): income body `{ title, amount, date, category_id?, description? }`, expense body swaps `title`→`item_name`; category_id must match an in-branch category of the matching type (`422`). **Nav fix:** the sidebar Finance item gated on a non-existent `finance.view` slug — repointed to `income.manage` (+ `expense.manage`/`asset.manage`). Super-admin branch scoping via the topbar switcher (no screen-local branch filter — the list endpoints don't accept one). The shared `CategorySelect` is a dependency 5.6 (categories management) will also consume. `tsc` + `lint` (0 errors, pre-existing warnings only) + `build` pass.

- Task 6.1 (`tasks/task-6.1-id-cards.md`): **done**. First screen of the **Documents** module (feature-spec 17). **Single ID card** now downloads from the student-detail **ID card** tab: `components/documents/id-card-panel.tsx` (gated on `idcard.generate`; a non-active/TC student shows a "no card available" note since the API 422s) triggers `GET /students/{id}/id-card` and saves the streamed PDF — replaces the old ComingSoon placeholder in `student-detail.tsx`. **Batch** lives on a new **`/documents`** route (`app/(app)/documents/`, `DocumentsPage` gated on `idcard.generate`): `components/documents/id-card-batch.tsx` picks session + class (+ optional section), queues `POST /id-cards/batch` (**202** `{ batch_id, status }`), then **polls** `GET /id-cards/batch/{batch}` with backoff (`useIdCardBatchStatus`, `refetchInterval` grows `1.5s→6s`, stops on terminal, `gcTime:0` so a new batch id never reads a stale terminal status) showing an indeterminate progress indicator, and downloads the merged PDF via `GET /id-cards/batch/{batch}/download` once `done`. Progress/done/failed + poll-transport-error states all present with retry/reset. PDFs are **streamed, never rendered client-side** — the blob endpoints use the raw `http` client (`responseType:'blob'`) + `saveBlob`/`filenameFromContentDisposition` (`lib/download.ts`, fallback names `idcard-{admission_no}.pdf` / `idcards-batch-{id}.pdf`). Hooks in `hooks/documents/use-id-cards.ts` (`useDownloadIdCard`, `useCreateIdCardBatch`, `useIdCardBatchStatus`, `useDownloadIdCardBatch`); types in `types/document.ts` (`IdCardBatchStatus`/`Input`/`Created`/`State`, `isTerminalBatchStatus`). Contract confirmed against backend `~/Herd/app-api` (`IdCardController`/`IdCardService`/`BatchIdCardRequest`, `routes/api/v1/students.php`): batch body `{ class_id, section_id?, session_id }` — the class/section/session **public_id hashes** the academic selectors emit are translated to internal ids by the global `ResolvePublicIds` middleware, so they're sent as-is; empty cohort → 422, download 409 until ready. **Nav fix:** the sidebar **Documents** item gated on a non-existent `documents.view` slug (same latent bug as the old `finance.view`) — repointed to `idcard.generate` (+ `tc.issue`/`tc.view` for the coming TC task). Super-admin branch scoping via the topbar switcher (the class list scopes through `useClasses`/the interceptor; the batch endpoint keys the cohort off the caller's branch, so no screen-local branch filter — mirrors 5.4). The student **self-service** profile ID-card tab (`profile-view.tsx`) is intentionally left as a placeholder — there's no self endpoint (single card requires `idcard.generate`). `tsc` + `lint` (0 errors, pre-existing warnings only) + `build` (`/documents` emitted) pass.

- Task 6.1 **follow-up — single ID card redesigned as a client-rendered preview + PDF** (imported "Student ID Card" Claude Design): the student-detail **ID card** tab now shows a **live, pixel-exact preview** of the design and downloads/prints it as a **client-generated PDF** (matching the money-receipt pattern), replacing the backend `GET /students/{id}/id-card` stream for the single card. `components/documents/id-card-paper.tsx` recreates the design's **two sides** (front identity + back terms/contact), each a fixed 324×512 card on the design's light canvas, as a **fixed-palette** paper (ignores app theme, inline literal styles, `--font-sans`/`--font-mono`/`--font-bengali` = Geist/Geist Mono/Noto Sans Bengali — exactly the design's faces): purple gradient bands, circular photo (initials-avatar fallback), details box, deterministic seeded barcode, Bengali motto, HJAMS branding constants reused from `receipt-paper.tsx`. `id-card-document.ts` (`downloadIdCardPdf`/`printIdCardPdf`) renders it via the shared `components/invoices/paper-pdf.ts` (html-to-image → jsPDF, content-fit page; the cards keep their radius/shadow since the flatten only targets `.invoice-paper-root`). Data maps from the full `Student` profile (name, class+section, roll, admission_no→Student ID, DOB, session→Valid thru, father→Guardian name/phone); **blood group** shows `—` (not in the profile contract). The **student photo** is resolved to a `data:` URL first via `usePhotoDataUrl` (`hooks/documents`, `fetch`→`FileReader`, `null` on CORS/missing) so the preview and PDF are identical and the rasterizer never taints on a cross-origin image. `IdCardPanel` now takes the whole `Student` (wired from `student-detail.tsx`); the old `useDownloadIdCard` single-stream hook was removed (the **batch** flow still uses the backend merged-PDF template, unchanged). Verified the rendered card against the design via headless-Chrome screenshot (front + back match). `tsc` + `lint` (0 errors; the two required raw-`<img>` warnings suppressed inline as in the sibling papers) + `build` pass.

- Task 6.2 (`tasks/task-6.2-transfer-certificates.md`): **done**. Second screen of the **Documents** module (feature-spec 17). **Issue** lives on the student-detail **TC** tab (`components/documents/student-tc-panel.tsx`, replacing the old ComingSoon placeholder in `student-detail.tsx`): a live (non-TC) student shows an **Issue transfer certificate** action (staff `tc.issue`) → `components/documents/issue-tc-dialog.tsx` (RHF+Zod, `reason` required ≤255 + `issue_date` required date defaulting to today) behind an **explicit irreversible-warning** confirmation; on issue → `POST /students/{id}/tc` (**201** returns the TC), the returned stored PDF **downloads**, and the mutation invalidates `["students"]`+`["tcs"]` so the profile flips to **TC** status (an info badge + retirement notice already present in `student-detail.tsx`). A **TC student**'s tab then surfaces the issued certificate (`IssuedTc`, looked up via `GET /tcs?search={admission_no}` matched on `student.id`) with its stored-PDF download and a link to the full record; a viewer without `tc.view` (e.g. a linked parent) sees the issued note only. **TC list** lives on the existing **`/documents`** page (`documents-page.tsx` now gates on `idcard.generate` **OR** `tc.view` and renders each section for the permission it holds): `components/documents/tc-list.tsx` is a paginated, searchable (tc_no/name/admission_no) ledger with an inclusive issue-date range, each row linking to the detail + offering the PDF; all four states present, responsive (table ≥ md, cards below). **Detail** is a new route `app/(app)/documents/tcs/[id]/` (gated on `tc.view`) → `components/documents/tc-detail.tsx` (shared detail-kit hero + certificate card + PDF download; out-of-branch id → not-found). PDFs are **streamed, never rendered client-side** (`code-standards.md`) — `useDownloadTc` uses the raw `http` client (`responseType:'blob'`) against `/tcs/{id}/pdf` (by tc id through the client base, not the resource `pdf_url` which already carries `/api/v1`) + `saveBlob`/`filenameFromContentDisposition` (fallback `tc-{tc_no}.pdf`). Hooks in `hooks/documents/use-tcs.ts` (`useTcs`, `useTc`, `useIssueTc`, `useDownloadTc`); types in `types/document.ts` (`TransferCertificate`, `TcStudent`, `IssueTcInput`, `TcListParams`). Contract confirmed against backend `~/Herd/app-api` (`TransferCertificateController`/`TransferCertificateService`/`IssueTcRequest`/`ListTcsRequest`/`TransferCertificateResource`, `routes/api/v1/students.php`): issue body `{ reason, issue_date }`, `store`→201 with the TC + embedded `StudentListResource` (id=public_id, class/section/roll from the current enrollment) + `pdf_url`; a student who already holds a TC → **409** ("Transfer certificate already issued", surfaced in the dialog banner); list filters `from`/`to`/`search`/`per_page`; `tc.issue` guards issue, `tc.view` guards list/detail/pdf; out-of-branch `{student}`/`{tc}` ids 404 via `BranchScope`. Super-admin branch scoping via the topbar switcher (folded into the `useTcs`/`useTc` keys, no screen-local branch filter — mirrors the ID-card/finance screens). `tsc` + `lint` (0 errors, pre-existing warnings only) + `build` (`/documents/tcs/[id]` emitted) pass.

- Task 6.2 **follow-up — TC rendered as the imported Claude Design (client PDF)**: the imported **"Transfer Certificate"** design (project `97bd3e51-a039-4b4c-9b8b-b7211616543d`, `Transfer Certificate.dc.html`, read via the `DesignSync` MCP) is now implemented **exactly** — mirroring the ID-card follow-up, the TC is a **client-rendered, pixel-faithful paper** (`components/documents/tc-paper.tsx`: a **fixed-palette** A4 794×1123 page — ignores app theme, inline literal design values, `--font-sans`/`--font-mono`/`--font-bengali` = Geist/Geist Mono/Noto Sans Bengali; navy `#1b3a63` header rule + Ref No./Date-of-issue block, underlined uppercase title + Bengali subtitle `ছাড়পত্র / স্থানান্তর সনদ`, the certification statement, an 8-row **Student record** grid, **General conduct** (green) + **Reason for leaving** boxes, closing paragraph, three signature lines, Bengali motto + invalid-without-seal footer; carries the `invoice-paper-root` class so the exporter flattens its border/radius/shadow). It's shown as a **live scaled-to-fit preview** and downloaded/printed as a **client PDF** through the shared `components/invoices/paper-pdf.ts` (html-to-image → jsPDF, content-fit page) via `tc-document.ts` (`downloadTcPdf`/`printTcPdf` + `buildTcPaperData`). A shared `tc-certificate-view.tsx` (preview + Print/Download) is rendered by **both** the TC **detail** screen (`tc-detail.tsx`, now just the certificate — no more hero/rows) and the student **TC tab** (`student-tc-panel.tsx` `IssuedTc`); issuing a TC downloads this client PDF (built from the returned TC + the full profile) instead of the streamed one. Data maps from the TC + the **full student profile** (fetched via `useStudent(tc.student.id)` on the detail route since the TC's embedded `StudentListResource` omits **date of birth / admission date**); `className`/`roll` prefer the profile's enrollment then the TC summary; `issue_date`/DOB/admission render in the design's long form ("12 July 2026") via a local `Intl` formatter. Fields the data model doesn't track — **general conduct, last examination taken, promoted-to class** — render as the em-dash marker rather than being invented (same honesty as the ID card's blood-group). The header logo reuses the app's `/branding/hjams-school-seal.svg` seal (the design's `logo.jpeg` isn't an app asset). The streamed backend PDF (`GET /tcs/{tc}/pdf`, `pdf.tc` blade) stays intact and is still used for the **quick per-row download** in the TC list (`useDownloadTc`). Verified the design layout via headless-Chrome screenshot of the design's own markup (header/title/record-grid/conduct/signatures/footer all match). `tsc` + `lint` (0 errors, pre-existing warnings only) + `build` pass.

- Task 6.1 **follow-up — two ID-card render fixes**: (1) **Back card clipped in the PDF** — the shared `components/invoices/paper-pdf.ts` hardcoded `orientation: "portrait"`, so jsPDF **swapped** the page dimensions for the ID card's *wide* two-card row (landscape aspect), making the page narrower than the embedded image and clipping its right ~18% (the right half of the back card). Fixed by deriving the orientation from the content aspect (`PAGE_WIDTH_PT >= pageH ? "landscape" : "portrait"`) — tall papers (receipts, mark sheets) stay portrait unchanged; the ID card becomes landscape and fits. Reproduced + confirmed via headless-Chrome (page `372×476` clipping a `452`-wide image → `476×372` fitting it). (2) **Student photo missing** — `photo_url` is on the cross-origin media host (`app-api.test`), so the browser `fetch` used to inline it into the client PDF was CORS-blocked (falling back to initials). Added a **same-origin image proxy** `app/api/image-proxy/route.ts` (scoped to the configured API host only — no open-proxy/SSRF) + `lib/image-proxy.ts` `proxiedImageUrl`; `usePhotoDataUrl` now fetches the proxied URL, so the real photo shows in both the preview and the PDF. Also made the on-screen preview **scale-to-fit** its column (`FitToWidth`, ResizeObserver) so the two-card layout is never clipped/scrolled on a narrow detail tab. `tsc` + `lint` + `build` (`/api/image-proxy` emitted) pass.

- Task 5.6 (`tasks/task-5.6-categories.md`): **done**. Added a **Categories** tab to the Finance shell (`finance-management.tsx`, gated on `income.manage` OR `expense.manage` — the same guard `routes/api/v1/accounting.php` puts on category CRUD; `?tab=categories` persisted like the other tabs). `components/finance/categories-list.tsx` is a paginated, **type-filterable** manager (a `Select` scoping to All types / Income / Expense — the backend `GET /categories` only supports the `type` filter + `per_page`≤100 + `page`, no search) with a create/edit dialog and delete-with-confirm; table ≥ md + mobile cards, all four states present, each row's type shown via `StatusBadge` (income→success, expense→neutral). `components/finance/category-form-dialog.tsx` (RHF + Zod, one component both modes) edits just `{ name, type }` — the `(branch, name, type)` uniqueness clash comes back as a `422` on `name` ("Category already exists") mapped to the field; `422` → field/banner via the shared `applyFieldErrors`/`FormBanner`. **Delete** surfaces the backend `409` ("Category is in use") message when a category is still referenced by income/expense rows, keeping the confirm open. Hooks: `hooks/finance/use-categories-list.ts` (`useCategoriesList`, paginated via `requestPaginated`, `type`+`page`+super-admin `branch` in the key, `keepPreviousData`, `STALE_TIME.REFERENCE` — kept **separate** from the existing `useCategories`, which stays the unwrapped cache-long `CategorySelect` source) and `use-category-mutations.ts` (`useCreateCategory`/`useUpdateCategory`/`useDeleteCategory`, each invalidating `["categories"]` so the manager list **and** every cached `CategorySelect` refetch together — a category added/renamed/removed here appears in the income/expense/asset forms immediately). Types in `types/finance.ts`: `CategoryInput` + `page` on `CategoryListParams`. Contract confirmed against backend `~/Herd/app-api` (`CategoryController`/`Store|Update|ListCategoriesRequest`/`CategoryService`, `routes/api/v1/accounting.php`): body `{ name, type }`, `POST`→201, `DELETE`→409 when in use, out-of-branch ids 404 via `BranchScope`. No new route/nav item — categories live inside the existing `/finance` screen. `tsc` + `lint` (0 errors, pre-existing warnings only) + `build` (`/finance` emitted) pass.

- **Global branch switching & creation (imported "Branch Switcher" design, cross-cutting)**: **done**. Reworked the branch model from single-branch-per-user into **multi-branch access** and rebuilt the switcher as a **sidebar** control matching the imported design, plus a **post-login branch picker**. **Backend (`~/Herd/app-api`):** branches gained `name_bn` (migration) + a `logo` media collection (spatie, mirrors `User` photo) surfaced as `logo_url` on `BranchResource`; `StoreBranchRequest`/`UpdateBranchRequest` accept `name_bn`+`logo` (multipart; update spoofs `PUT` via `_method`), `BranchController` stores the logo. New `branch_user` pivot + `User::allowedBranches()`/`canAccessBranch()`/`accessibleBranches()` (home branch always implicitly allowed; backfill migration seeds existing users' home branch). New `App\Support\BranchContext` (reset per request by `ResolveBranchContext` middleware, registered after `ResolvePublicIds`) resolves the active branch — super admin → requested `branch_id` or null; others → requested branch **iff accessible**, else home. `BranchScope`, the `BelongsToBranch` create-stamp, `SettingsRepository`, `IdCardService`, `ParentService`, `DashboardService`/`AcademicStructureService` cache keys, and the `Store{Student,Class,Teacher,CheckinIp}Request`/`ReportFilterRequest` branch resolvers all read `BranchContext::current()` instead of `Auth::user()->branch_id` (identical for single-branch users, switches for multi-branch). `/auth/me` (+ login/profile) returns the caller's accessible branches as `user.branches` via `UserResource` (self-only guard). New `PUT /users/{user}/branches` (super-admin, `role.manage`) syncs the pivot; `branch_ids` added to `ResolvePublicIds` array resolution; `UserAccountResource` exposes `allowed_branch_ids`. Tests: `tests/Feature/Api/V1/BranchSwitchTest.php` (5 passing: resource shape, `/auth/me` accessible set, allowed-branch switch honored, disallowed→home fallback, assignment endpoint); **full suite shows zero new failures vs. the pre-existing baseline** (113 pre-existing failures unrelated to this work — the codebase's public_id-vs-id assertion drift). **Frontend (`apps/web`):** `types/branch.ts` +`name_bn`/`logo_url`, `types/auth.ts` `AuthUser.branches`. `branch-provider.tsx` generalized beyond super admin — active branch = selected (if accessible) else home, persisted to `localStorage` + a per-session "chosen" flag driving the picker; exposes `branches`, `currentBranch`, `recentBranches`, `canSwitch`, `needsBranchSelection`, and still `isSuperAdmin`/`activeBranchId`/`branchParam` for existing consumers; the API bridge (`lib/api/branch`) now carries the active branch for **all** users. `use-branch-mutations.ts` sends `name_bn`+logo as multipart and invalidates `["auth","me"]` so the switcher reflects new branches immediately. New components in `components/branch/`: `branch-avatar.tsx` (logo/initials on a deterministic palette color, `lib/branch/visual.ts`), `branch-row.tsx`, `branch-switcher.tsx` (sidebar trigger + Popover: search, Recently used, All branches with count, "Create new branch" gated on `branch.manage`, "Switched to…" toast), `create-branch-modal.tsx` (English + Bengali name + logo upload; `code` auto-derived with a numeric-suffix retry on 422 clash; auto-selects the new branch), and `branch-select-dialog.tsx` (non-dismissable post-login picker shown when ≥2 accessible branches and unchosen; single-branch users auto-select). Mounted in the **sidebar top** (replacing the brand block, `sidebar.tsx`) + the mobile drawer (`app-shell.tsx`); **removed from the topbar** (`topbar.tsx`); old `components/app-shell/branch-switcher.tsx` deleted; picker mounted in `app/(app)/layout.tsx`. `tsc` + `lint` (0 errors) + `build` pass. **Colors follow app theme tokens** (design's literal light-mode hex mapped to tokens per `ui-context.md`/invariant 7), except the data-driven branch-avatar accent. **Remaining follow-up:** a frontend multiselect to assign allowed branches to a user (backend endpoint + `allowed_branch_ids` are ready).

## How To Work (per session)

1. Open this tracker, find the first unchecked task.
2. Read that task's ticket in `tasks/` — tickets are self-contained (objective, endpoints consumed, screens/components, behavior, gating, states, check-when-done).
3. Consult the named `feature-specs/*.md`, `docs/api/*.md`, `ui-context.md` only when the ticket references them.
4. Implement → states (loading/empty/error/success) present → `npm run build` green → tick the box and set the ticket Status `done`.

Each feature-spec (`feature-specs/NN-*.md`) is the module overview; the `tasks/*.md` tickets below are the per-screen units of work derived from it and the backend API. Every endpoint in `docs/api/*.md` is mapped to a task. Never start a task before the ones above it in the same phase are done; never start a phase before the previous one is done.

## Roadmap (36 tasks)

### Phase 1 — Foundation — `feature-specs/01..05`
- [x] [1.1](tasks/task-1.1-design-system.md) — Design system: tokens, two-axis themes, fonts, switcher
- [x] [1.2](tasks/task-1.2-ui-primitives-state.md) — shadcn primitives + shared state/feedback components
- [x] [1.3](tasks/task-1.3-api-client.md) — API client (Axios + envelope + errors) + TanStack Query + types
- [x] [1.4](tasks/task-1.4-auth.md) — Auth: login, session cookie, permissions context, route guard, change-password
- [x] [1.5](tasks/task-1.5-app-shell.md) — App shell: sidebar + topbar + nav + user menu + branch switcher
- [x] [1.6](tasks/task-1.6-global-search.md) — Topbar global command search
- [x] [1.7](tasks/task-1.7-dashboard.md) — Dashboard

### Phase 2 — Academic & People — `feature-specs/06..09`
- [x] [2.1](tasks/task-2.1-academic-selectors.md) — Academic shared selectors + read hooks
- [x] [2.2](tasks/task-2.2-academic-management.md) — Sessions / classes / sections / subjects management
- [x] [2.3](tasks/task-2.3-assignments-branches.md) — Teacher assignments + branches management
- [x] [2.4](tasks/task-2.4-teachers.md) — Teachers: list, detail, create/edit, status, photo, resend
- [x] [2.5](tasks/task-2.5-public-admission.md) — Public admission form + status check (standalone)
- [x] [2.6](tasks/task-2.6-admissions-review.md) — Admissions review: queue, detail, approve/reject
- [x] [2.7](tasks/task-2.7-students.md) — Students: list, detail, edit, photo, status, enrollments
- [x] [2.8](tasks/task-2.8-parents.md) — Parents: list, create, link/unlink, my-students

### Phase 3 — Attendance — `feature-specs/10..11`
- [x] [3.1](tasks/task-3.1-student-attendance-entry.md) — Student attendance entry (roster grid)
- [x] [3.2](tasks/task-3.2-student-attendance-sheets.md) — Attendance sheets (class + per-student/self/children)
- [x] [3.3](tasks/task-3.3-teacher-attendance.md) — Teacher attendance: check-in/out + admin correction

### Phase 4 — Exams, Results, Promotion — `feature-specs/12..14`
- [x] [4.1](tasks/task-4.1-exams.md) — Exams: list, create/edit
- [x] [4.2](tasks/task-4.2-mark-entry.md) — Mark entry grid (grades read-only from API)
- [x] [4.3](tasks/task-4.3-results.md) — Results: search, view, self, generate/publish
- [x] [4.3.1](tasks/task-4.3.1-public-results.md) — Public result lookup
- [ ] [4.4](tasks/task-4.4-result-pdfs.md) — Result-sheet PDF download ⚠️ blocked on backend 8.4
- [x] [4.5](tasks/task-4.5-promotions.md) — Promotion: preview, bulk, individual

### Phase 5 — Finance — `feature-specs/15..16`
- [ ] [5.1](tasks/task-5.1-fee-structures.md) — Fee structures + invoice generation
- [x] [5.2](tasks/task-5.2-invoices.md) — Invoices: list, detail, my-invoices
- [x] [5.3](tasks/task-5.3-payments.md) — Payments: online (SSLCommerz), local, receipt
- [x] [5.4](tasks/task-5.4-finance-income-expenses.md) — Finance: income + expenses
- [ ] [5.5](tasks/task-5.5-finance-assets.md) — Finance: assets + summary
- [x] [5.6](tasks/task-5.6-categories.md) — Finance categories management

### Phase 6 — Documents, Reports, Settings — `feature-specs/17..19`
- [x] [6.1](tasks/task-6.1-id-cards.md) — ID cards: single + batch (poll/download)
- [x] [6.2](tasks/task-6.2-transfer-certificates.md) — Transfer certificates: issue, list, PDF
- [ ] [6.3](tasks/task-6.3-reports.md) — Reports: filters, all report types, PDF export
- [ ] [6.4](tasks/task-6.4-settings-global.md) — Settings: global (identity, session, credentials, grading scale, toggles)
- [ ] [6.5](tasks/task-6.5-settings-branch.md) — Settings: per-branch (info, IP whitelist, class fees)
- [ ] [6.6](tasks/task-6.6-access-control.md) — Access control: assign roles to users + permissions to roles ⚠️ blocked on backend 15.1

## Completed

- [4.3 results — "Result publication" tab (per-exam generate+publish)] Added a fourth tab at the end of the results-screen tab strip (`apps/web/components/results/results-page.tsx`, `ResultPublicationPanel`) exposing a Session + Semester + Class form and a single "Publish results" button (permission-gated on `RESULT_GENERATE`). Because the API guarantees one exam per (session, class, type) tuple per branch, those three fields resolve exactly one exam via `useExams`; the button then runs `useGenerateExamResults` followed by `usePublishExamResults` inside a confirm dialog so entered marks are recomputed into result rows and published, making them searchable for everyone. This wires up the previously-defined-but-unused per-exam generate/publish hooks that were the missing pipeline stage between mark entry and result search. Super-admin sees an optional Branch filter; a "No exam exists for this selection" message shows when the tuple resolves nothing. Verification: `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean aside from the workspace's existing 18 warnings.
- [4.3 result marksheet — exam year in title + "Held in the Month of …" line (full-stack)] The transcript heading now reads like the reference design, e.g. "SECOND SEMESTER EXAMINATION, 2016" with an italic "(Held in the Month of June - July)" beneath it. **Backend** (`~/Herd/app-api`): `Exam::heldInLabel()` derives a month-range label from the exam `start_date`/`end_date` ("June, 2026" / "June - July, 2026" / "December, 2025 - January, 2026"); `ResultService::bundle()` eager-loads `exam.session`, and `ResultBundleResource` now emits `exams[].year` (session name) + `exams[].held_in`; `ResultService::publicResult()` eager-loads the exam date range and `PublicResultResource` emits `student_information.held_in`. While finishing the public payload I also completed a half-wired sibling field the frontend type + test already expected: the public result now bundles the server-owned `grading_scale` (high band first) since the authed `GET /grading-scales` is unreachable for guests. **Frontend** (`apps/web`): `PerExamResult` gained `year`/`held_in` and `PublicResultStudentInformation` gained `held_in` in `types/result.ts`; `ExamMarkSheet` (`result-bundle-panel.tsx`) and `PublicResultView` (`public-result-lookup.tsx`) build the title as `${label} Examination, ${year}` when the session year is known (falling back to `${label} Result`) and pass `examMonth`. The shared `ResultMarkSheetPaper` drops the duplicate year from the month line when the title already carries it (`monthWithoutDuplicateYear`). Verification: backend `php artisan test --filter="Result|Exam"` 75 green; Pint clean; frontend `npm run typecheck --workspace apps/web` clean; eslint on touched files clean.
- [4.4 result-sheet download — foreignObject raster, content-fit page, high DPI] Replaced the html2canvas raster path in `apps/web/components/results/result-mark-sheet-document.ts` with `html-to-image`'s `toCanvas`, which serializes the DOM into an SVG `<foreignObject>` and lets the browser paint it — so table borders and cell vertical-centring in the downloaded/printed sheet now match the on-screen view exactly instead of html2canvas's doubled/thick borders and mis-aligned values. The jsPDF page is now sized to the content aspect (A4 width, height derived from the canvas) so the transcript ends where the content ends and no trailing white space is emitted, and the raster is captured at a fixed `pixelRatio` of 3 for a crisp PDF regardless of the device DPI. html2canvas stays a dependency (still used by the admissions application PDF). Verification: `npm run typecheck --workspace web` clean; `eslint components/results/result-mark-sheet-document.ts` clean. (Runtime foreignObject export is browser-only and not exercised headlessly.)
- [4.4 result-sheet download — client PDF now matches web sheet more closely] Extracted the shared transcript body into `apps/web/components/results/result-mark-sheet-paper.tsx`, then pointed both the on-screen `ResultMarkSheet` and the temporary client-side exporter (`result-mark-sheet-document.ts`) at that same paper component so the download path stops drifting from the web view. The PDF renderer now waits for fonts/images, captures the shared paper + footnote as a PNG instead of a JPEG, and explicitly middle-aligns grading-legend and subject-table cells, which fixes the reported heavy-border / off-center table-value look in downloaded mark sheets while preserving the existing client-side fallback until backend 8.4 ships the real streamed PDF endpoint. Verification: `npm run typecheck --workspace web` clean; `npm run lint --workspace web -- components/results/result-mark-sheet.tsx components/results/result-mark-sheet-paper.tsx components/results/result-mark-sheet-document.ts` clean.
- [4.3 admin/student result marksheet — watermark aligned with public sheet] The admin and student result marksheets now use the same watermark presentation as the public marksheet: `ExamMarkSheet` in `apps/web/components/results/result-bundle-panel.tsx` no longer passes `passed` / `published` into the shared `ResultMarkSheet`, so those screens stop adding the diagonal `FAILED` / `PROVISIONAL` text overlay and keep the centered school-seal watermark only. Result status still remains visible in the surrounding result badges/cards; this change is strictly about matching the marksheet visual format across public, admin, and student surfaces. Verification: `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean aside from the workspace’s existing 18 warnings.
- [4.3.1 public result marksheet — grading legend height/placement aligned] The shared mark-sheet particulars row (`result-mark-sheet.tsx`) now uses a stretched legend column instead of top-aligning the grading table at its natural height. The `Range of Marks / Grade / Point` table now expands to the full height of the student-particulars block, so the public marksheet’s static legend sits in the same visual position and vertical span as the admin result marksheet even with the public-only extra fields. Mirrored the same structure in `result-mark-sheet-document.ts` so print/download PDFs match the on-screen placement. Verification: `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean aside from the workspace’s existing 18 warnings.
- [4.3.1 public result marksheet — static grading legend] The public result transcript now renders the school-approved `Range of Marks / Grade / Point` table from a fixed frontend scale in `apps/web/components/results/public-result-lookup.tsx` instead of depending on `result.grading_scale`. The rows match the supplied design exactly: `80–100 A+ 5.00`, `70–79 A 4.00`, `60–69 A- 3.50`, `50–59 B 3.00`, `40–49 C 2.00`, `33–39 D 1.00`, `0–32 F 0.00`. Because the shared `ResultMarkSheet` drives screen + print/download, the static legend now appears consistently on the public sheet and its generated PDF. Admin/student result sheets are unchanged and still use their existing grading-scale flow. Verification: `npm run typecheck --workspace web` clean.
- [4.4 result-sheet branding defaults] The shared result mark sheet now uses a bundled school seal by default (`apps/web/public/branding/hjams-school-seal.svg`) instead of the temporary placeholder badge, and that same seal is passed through to the client-side PDF/print renderer so on-screen, downloaded, and printed sheets stay in sync. Added a faint logo watermark behind the mark-sheet body while preserving the existing diagonal text watermark for provisional/failed copies; follow-up refinements expanded it with a broader halo, then moved the logo watermark up so it begins around the exam-title block ("Second Semester Result") while leaving the diagonal text watermark in its lower centered position. This is strictly presentation work on the temporary client-rendered sheet; backend-streamed PDF download for task 4.4 remains blocked on backend 8.4. Verification: `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean aside from the workspace's existing 18 warnings; `npm run build --workspace web` still stops in the sandbox when Next.js tries to fetch its configured Google fonts.
- [2.2 class form — branch selectable + required on create and edit] `class-form-dialog.tsx` previously showed the Branch select only for super-admin create; edits never sent `branch_id`. The Branch field now renders for super admins on both create and edit, is required (inline "Select a branch" error when empty), and `branch_id` is included in both the create and update payloads. Edit prefills from the class row's `branch_id` (added defensively to `SchoolClass`) falling back to the active branch. Auto-scoped (non-super-admin) users are unchanged — `/branches` is super-admin-only, so their branch stays API-enforced with no field shown. `tsc --noEmit` clean.
- [2.6 admissions approve — class fixed to desired class] In the approve dialog (`approve-dialog.tsx`), when the applicant entered a desired class the Class field is fixed to it: rendered as a read-only input showing the desired class *name* (not a `ClassSelect`), so it displays correctly even when the desired class isn't among the reviewer's branch-scoped `/classes` options (the earlier disabled-select version could show a blank/wrong entry when the seeded `public_id` matched no option). The form still submits the desired class `public_id`, and `SectionSelect` fetches sections for it via `/classes/{id}/sections`. A `ClassSelect` is offered only when the application has no desired class. `tsc --noEmit` clean.
- [4.3.1 public result lookup — button alignment fix] The Search button column (`public-result-lookup.tsx`) used `xl:self-end`, so field validation messages growing the grid row pushed the button below the inputs. It is now `self-start` behind an invisible `label[data-slot="form-label"]` spacer (xl-only) that inherits the scoped `.admission-fields` label CSS, keeping the button level with the inputs regardless of error text. Follow-up: field heights on the public surfaces were inconsistent — the scoped `.admission-fields` CSS only targeted `[data-slot="input"]`/`[data-slot="select-trigger"]`, so the native `<select>` inside `AcademicSelect`/`LocationSelect` stayed 40px next to 46px inputs, and the Search button was 44px. Added a scoped `.admission-fields select` rule (46px min-height, 10px radius, 1.5px border, chevron-safe right padding; color left to utilities so muted placeholder/disabled/invalid states hold) with matching focus ring, and set the Search button to `h-[46px]`. `tsc --noEmit` clean.
- [4.3.1 public result lookup — branch_id fix] Fixed the public result search always failing with a 422: `GET /api/v1/public/results` requires `branch_id` (`PublicResultRequest`), but the frontend collected it in the form and then dropped it from the API request. `PublicResultLookupParams`, `toLookupParams`, and the hook's param cleaner now include `branch_id`. Verified against the live API (422 "branch id required" → correct 404/success paths) and `tsc --noEmit`. Dev data: regenerated + republished exam 10's results (1 → 47 published; 166 enrollments skipped for incomplete marks) so the public form has findable results — e.g. roll 1 / Madani PathShala / Class 2 / 2026 / first semester; the original lone published row sits on an inconsistent enrollment (branch-1 student in branch-3 class 12) and stays unreachable. Backend: the public lookup's three not-found paths (unknown session year, no matching enrollment, no published result) now abort with "No published result was found for those details…" instead of the generic "Resource not found.", and the `bootstrap/app.php` 404 renderer surfaces explicit `abort(404, …)` messages while still masking model/route internals. Verified over HTTP (404 copy, 200 found case, unknown route still generic); `PublicResult*` (6) and all `*Result*` (42) feature tests pass; Pint clean.
- [4.3.1 public result lookup] Added the standalone unauthenticated `/results/public` route outside the dashboard shell, reusing the public admission themed surface with route-level loading/error states. The lookup form is RHF+Zod validated (digits-only roll number, separate branch dropdown from `GET /public/settings`, branch-scoped class public id, 4-digit year, semester options from the exam types) and commits submissions into `?roll_no=&branch_id=&class_id=&year=&semester=` so refresh/back preserves the lookup while the API request still sends only the documented `class_id`. A new token-free `publicApi` client shares the standard envelope/error normalization without attaching bearer tokens or branch context, and `usePublicResultLookup` calls `GET /public/results` only after submit. Successful responses render every `student_information` field with the app date formatter plus responsive subject table/cards for `subject_code`, `subject_name`, `marks`, and `grade`, displaying result/marks/grade exactly as returned with no client-side result computation. `422` maps back to inline fields; `404`/empty success renders the generic not-found copy; network/5xx errors render a retryable error panel. Verification: `npm run typecheck --workspace web`, `npm run lint --workspace web` (0 errors, existing 17 warnings), and `npm run build --workspace web` pass; build required network access for configured Next/font Google font fetching.
- [4.3 results search/view/self + generate/publish] Implemented the authenticated `/results` route with route-level loading/error states and the real backend result contract (`ResultController`, `AnnualResultController`, `ResultBundleResource`, `ExamResultResource`) because `docs/api/results.md` is absent. New result types and TanStack hooks cover `GET /results/search`, `GET /enrollments/{id}/results`, `GET /me/results`, `GET /exams/{exam}/results`, `POST /exams/{exam}/results/generate|publish`, and `POST /annual-results/generate|publish`; query keys include filters and branch context, and mutations invalidate results/exams. Staff with `result.view` can search by admission no or full session/class/section/roll coordinates, browse paginated per-exam result rows with section/verdict filters, and view returned subject marks, GPA, grade, annual result, pass/fail, and published flags exactly as the API returns them. `result.generate` gates all generate/publish actions with confirmation dialogs and success/error toasts. Students and parents without `result.view` use `/me/results`; parent-linked children can be selected from `/me/students`, and existing `/profile?tab=results`, student detail Results tabs, and parent `/results?student=` links now render live results instead of placeholders. Corrected the Results sidebar permission slug from `results.view` to the backend `result.view`. Verification: `npm run typecheck --workspace web`, `npm run lint --workspace web` (0 errors, existing 17 warnings), and `npm run build --workspace web` pass; build required network access for configured Next/font Google font fetching.
- [branch scope defaults + class branch create] Super-admin branch context now starts every session in the consolidated **All branches** state instead of restoring a previous branch from local storage; choosing a concrete branch in the topbar still forwards `branch_id` through the existing API interceptor and invalidates branch-scoped queries, so students, teachers, parents, exams, attendance, academic selectors, and other branch-aware reads refetch for that branch. Class creation now requires a branch for super-admin users and sends `branch_id` on `POST /classes`; non-super-admin users still omit it and rely on API scoping. The app-shell brand text now uses the accent token (`text-brand`) to match the active accent. Verification: `npm run typecheck --workspace web`, `npm run lint --workspace web` (0 errors, existing 17 warnings), and `npm run build --workspace web` pass.
- [4.2 multi-subject mark entry design (full-stack)] Reimplemented `/marks` to the imported "Multi-Subject Mark Entry" design (Claude Design `d78bfe47…`): one grid for **all subjects of a class/section at once**, plus a dense **By subject** view, sharing a single draft. **Backend** (`~/Herd/app-api`): new `GET /exams/{exam}/marks/matrix?section_id=` (`MarkController@matrix` → `MarkService::matrix()` → `MarkMatrixResource`, validated by `MarkMatrixRequest`) returns the section's class subjects as columns + active roster, each student carrying one cell per subject (`obtained_marks`, `is_absent`). Added an **`is_absent`** column to `marks` (migration; `Mark` fillable + boolean cast) — an absent row is stored as `0` obtained marks with the flag so the grade resolves to the fail band and result generation still sums cleanly; `StoreMarksRequest` now accepts `marks.*.is_absent` (and `obtained_marks` is `required_if` not absent, range-check skipped for absent), `MarkService::saveBulk` handles it, and `MarkSheetResource`/`MarkMatrixResource` expose it. **Publish/lock** wired to the exam status: `POST .../marks/publish` (→ Published, freezes edits via the existing `409`) and `POST .../marks/unpublish` (→ Completed) on `MarkController`, gated `marks.entry`. `MarkSheetResource`/`MarkMatrixResource` also return `exam.status`. 3 new feature tests (matrix columns, absent stored as zero+flag, publish freezes→unpublish reopens) — `MarksEntryTest` 12→15 green; Pint clean. **Frontend** (`apps/web`): `useMarkMatrix` hook + `usePublishMarks`/`useUnpublishMarks`; `types/mark.ts` gained the matrix/absent shapes. `mark-entry-grid.tsx` rebuilt to the design — page header/breadcrumb + Exam/Class/Section selectors, a stats bar (marks-entered progress, class avg, top GPA, pass rate, failing, absent — **client-side previews** aggregated from server-resolved grade points, not authoritative), Matrix grid with **frozen Student / Total / GPA / Result columns** and a subject-average footer + arrow-key/Enter cell navigation, By-subject pills + dense table with a per-student **absent toggle**, search by **roll or name**, client pagination, a published/locked banner with "Unlock to edit", and a grading-scale legend driven by `GET /grading-scales`. Save persists every entered subject per-subject (per-row `422` mapped back to cells); Publish saves then locks. Grades/points shown are the API's (`resolveGrade` mirrors the server scale) — the client never invents grades. `tsc --noEmit` clean; `npm run build` passes.
- [teachers trash/delete (full-stack)] Teacher management now supports the soft-delete lifecycle end-to-end, mirroring the students/parents pattern. **Backend** (`~/Herd/app-api`): the `Teacher` model + table already had `SoftDeletes`/`deleted_at`; added the missing routes (`GET /teachers/trash`, `POST /teachers/bulk-delete|bulk-restore|bulk-force-delete`, `DELETE /teachers/{teacher}`, `POST /teachers/{teacher}/restore`, `DELETE /teachers/{teacher}/force` — literal/bulk paths registered before `{teacher}` binding, restore/force `withTrashed()`), all gated on a new **`teacher.delete`** permission (added to `PermissionSeeder` + the `admin` bundle in `RoleSeeder`, re-seeded). New `BulkTeachersRequest`, `TeacherController` methods (`trash`/`destroy`/`bulkDestroy`/`restore`/`bulkRestore`/`forceDestroy`/`bulkForceDestroy` + a `paginated` envelope helper), and `TeacherService` methods (`listTrashed` + a shared `filtered()` base query refactored out of `list()`, `delete`/`bulkDelete`/`restore`/`bulkRestore`/`forceDelete`/`bulkForceDelete`). Soft-delete disables the linked login + revokes tokens; restore re-enables it when the teacher is active; force-delete is blocked (409) when dependent `teacher_attendances` history exists, otherwise drops assignments → teacher (sections' `class_teacher_id` nulls via FK) → user. `TeacherResource` now emits `deleted_at`. Teacher test suite unchanged at 59 passed / 9 failed (the 9 are the pre-existing repo-wide numeric-id route-binding failures — identical before/after). **Frontend**: the `/teachers` list has a `teacher.delete`-gated per-row **Move to trash**, page-scoped checkbox selection, and a bulk move-to-trash bar with destructive confirmation; the detail hero overflow menu exposes Move to trash and returns to the list after success. Added TanStack mutations (delete / bulk-delete / restore / bulk-restore / force-delete / bulk-force-delete) and `useTrashedTeachers` (reuses exported `toTeacherQuery`). New `/teachers/trash` route + `TeachersTrash` component, gated on `teacher.delete`, with search, bulk selection, direct restore, and confirmed permanent delete. New `TEACHER_DELETE = "teacher.delete"` permission constant (the delete UI gates on the real backend slug, not the legacy `teachers.manage`); teacher rows carry optional `deleted_at`. Verification: `tsc --noEmit` + `eslint` clean; `php -l` clean on all backend files; tinker smoke test confirmed delete→trash→restore.
- [students trash/delete frontend] Student management now supports the backend soft-delete lifecycle. The `/students` list has `student.delete`-gated per-row **Move to trash**, page-scoped checkbox selection, and bulk move-to-trash with destructive confirmation; the detail hero also exposes Move to trash and returns to the list after success. Added TanStack mutations for delete / bulk-delete / restore / bulk-restore / force-delete / bulk-force-delete and a `useTrashedStudents` hook for `GET /students/trash`. New `/students/trash` route is permission-gated on `student.delete`, mirrors the live list filters, supports bulk selection, direct restore, and confirmed permanent delete while surfacing API conflict/errors (dependent history blocks force delete). Student list rows now carry optional `deleted_at`. Verification: `npm run typecheck --workspace web` clean; `npm run lint --workspace web` 0 errors (existing 17 warnings); `npm run build --workspace web` passes and registers `/students/trash`.
- [exams: full edit + delete + bulk delete (full-stack)] **Every exam is now editable** — removed the published-exam freeze (the old `409`) from `UpdateExamRequest`; name/dates/status are editable on any exam (identity columns session/classes/type stay prohibited; a published exam keeps its terminal status, so the frontend renders that field read-only and omits `status` from the payload). **Delete:** `DELETE /exams/{id}` (`destroy`) and **bulk** `POST /exams/bulk-delete` (`bulkDestroy`, accepts `ids` of public ids, branch-scoped so foreign ids are skipped). `ExamService::delete()` removes the exam's marks + per-exam results + class pivot in a transaction (those FKs restrict at the DB level); `bulkDelete()` loops it. Both gated on `exam.manage`. Tests added (edit published name, single delete + pivot cleanup, out-of-branch delete 404, bulk delete in-branch-only + required-ids, cascade of marks/results); full backend suite 469→474 passed, 127 failed unchanged (pre-existing repo-wide numeric-id route-binding pattern), no regressions. **Frontend:** `useDeleteExam`/`useBulkDeleteExam` hooks; the list gained a per-row Delete (trash) action and a selection model — a header/row/card checkbox (page-scoped selection, no UI checkbox primitive so a small native `SelectCheckbox` wrapper) plus a "N selected → Delete selected" toolbar; both single and bulk confirm via the shared destructive `DeleteDialog` (warns that marks/results are removed). The edit dialog is un-frozen (all fields editable; published status shown read-only). `npm run typecheck` clean, `npm run lint` 0 errors (17 baseline warnings), `npm run build` passes.
- [exams edit fix + required-field stars] Fixed exam edit appearing broken: it was caused by the list disabling the Edit action for `published` exams (and the seeded exams are all published), so every Edit looked dead — the backend `PUT /exams/{id}` itself works (verified over HTTP). Edit is now always openable; for a published (frozen) exam the dialog opens read-only with a clear "published and frozen" notice and a Close button (no Save), since the API rejects changes to published exams (409). Non-published exams edit name/dates/status as before. Also added a reusable required-field indicator: the shared `FormLabel` (packages/ui) gained an optional `required` prop that appends a red `*`, applied to the exam form's required fields (Session, Type, Classes, Name). Schema/data-model converted on the live MySQL dev DB via a new non-destructive ALTER migration (`convert_exams_to_multi_class`) after reverting the in-place edit of the original `create_exams` migration. `npm run typecheck` clean, `npm run lint` 0 errors (17 baseline warnings), `npm run build` passes.
- [exams multi-class scope (full-stack)] Reworked exams so one exam targets **multiple classes or all classes** instead of a single class — both backend (`~/Herd/app-api`) and frontend. **Backend:** dropped `exams.class_id` + the `(session, class, type)` unique index and added an `all_classes` boolean (edited the original `create_exams` migration; pre-release schema) plus a new `exam_class` pivot migration; `Exam` gained a `classes()` belongs-to-many, `all_classes` cast, and a `classIds()` helper (resolves the branch's classes when `all_classes`). `ExamResource` now returns `all_classes` + `class_ids` + `classes` (id/name) instead of `class_id`. `StoreExamRequest` accepts `class_ids` (required unless `all_classes`) or `all_classes` (super admin supplies `branch_id`), rejects cross-exam class overlap within a (session, type), and `UpdateExamRequest` additionally prohibits `class_ids`/`all_classes`. `ExamService` creates over the pivot and derives branch from the targeted classes (or active/super-admin branch); the list `class_id` filter matches covered-or-all exams. Downstream made class-aware: `MarkSheetRequest`/`StoreMarksRequest` validate against `exam->classIds()`, `ResultService::generateExamResults` grades each enrollment against its own class's subjects across the exam's classes, and `AnnualResultService::exams()` resolves exams covering a class (pivot or all). `ResolvePublicIds` middleware now also translates plural `class_ids` arrays of public ids. `ExamFactory` gained `forClass()`/`forClasses()`/`allClasses()`; `DemoSeeder` attaches the pivot. Tests updated to the new contract (ExamCrud rewritten incl. multi-class/all-classes/overlap/class-filter cases; marks/results/annual factory + route-key usages). Full backend suite improved 441→469 passed / 152→127 failed vs HEAD — no regressions; the remaining failures are a pre-existing repo-wide pattern (feature tests calling `/{model}` routes with numeric `->id` while binding is by `public_id`), unrelated to exams. **Frontend:** `types/exam.ts` swapped `class_id` for `all_classes` + `class_ids` + `classes`; new `ClassMultiSelect` chip control (multi-pick + "All classes" toggle) drives the create form; the form sends `class_ids` (or `all_classes`), keeps session/classes/type read-only on edit; the list shows a "Classes" column ("All classes" or the names from each row). `npm run typecheck` clean, `npm run lint` 0 errors (17 baseline warnings), `npm run build` passes.
- [exams list + create/edit (task 4.1)] New `/exams` route (gated on `exam.view`, with an access-denied state) renders `ExamsList`: a paginated, filterable table (Exam name, Type, Session, Class, Schedule date-range, Status badge) with a responsive card list < md, plus `loading.tsx`/`error.tsx`. Filters by session and class (shared `SessionSelect`/`ClassSelect`) and by type/status (shadcn `Select` with an "all" pass-through); changing any filter resets to page 1, set filters fold into the query key, and `keepPreviousData` keeps pages from flashing. The exam resource returns `session_id`/`class_id` as `public_id` hashes (not names), so the list resolves display labels from the cached `useSessions`/`useClasses` selector data. Create/edit happen in one `ExamFormDialog` (RHF + Zod, `422` → field errors + form-level banner, success toast + list refresh, no double submit). Create sets session/class/type/name/start_date/end_date (branch derived server-side from the class — no `branch_id` in the body, unlike teachers); on edit the identity columns (session/class/type) render read-only since the backend prohibits changing them, and only name/dates/status are editable — `published` is excluded from the status options (it's a separate publish flow, task 4.3) and the row's Edit action is disabled for already-published exams (frozen → `409`). Hooks live in `hooks/exams` (`useExams` list, `useCreateExam`/`useUpdateExam` writes invalidating `["exams"]`); types/labels/tones in `types/exam.ts`; permissions (`exam.view`/`exam.manage`) match the live backend routes. Also corrected the sidebar nav item permission from the non-existent `exams.view` to `exam.view` so the item actually surfaces. `npm run typecheck` clean; `npm run lint` 0 errors (existing 17 warnings, none from exam files); `npm run build` passes (`/exams` registered).
- [parent students nav + read-only detail] Parent accounts now get a "Students" item in the left sidebar (role-gated to `parent` via `roleOnly`, since parents hold no `student.view` permission) that routes to `/my-students`. That page is now a paginated **table that mirrors the admin students list** — same columns (Student with bilingual name, Admission no, Class / Section, Roll, Status badge; no actions column since parents are read-only), search + class/section filters, responsive card list < md. It reads the parent's linked children from `GET /me/students`; since that endpoint returns the full flat set and the staff `/students` table is permission-gated/unreachable for parents, the search/filter/pagination run client-side. To supply the extra columns, the backend `LinkedStudentResource` (`~/Herd/app-api`) was extended to mirror `StudentListResource` (added `admission_no`, `name_bn`, `roll_no`, `status`; `currentEnrollment` was already eager-loaded), and the frontend `LinkedStudent` type gained those optional fields. Each row links to `/my-students/{id}`, a parent-facing detail route reusing the shared `StudentDetail`. `StudentDetail` gained optional `backHref`/`backLabel` props (default `/students` / "Back to students") so the parent flow returns to `/my-students`, and now exposes **all section tabs** (Profile, Attendance, Results, Leaves, Tuition fees, ID card, TC) for the `parent` role — the section tabs are role-exposed since parents hold no staff permissions (the API authorizes each child resource for the linked parent; Attendance is live, the rest render coming-soon placeholders). The Profile stays read-only: edit/photo/status/resend remain gated on `student.update`, which parents lack. The API authorizes the read via `StudentPolicy::view` for linked parents; unlinked/out-of-branch/missing records render not-found. `npm run typecheck --workspace web` clean; `npm run lint --workspace web` 0 errors (existing 17 warnings); `npm run build --workspace web` passes.
- [admission approve parent relation select] Fixed the Base UI controlled-state warning in the admissions approval dialog by keeping the parent-relation select controlled for its full mounted lifetime (`null` empty value instead of `undefined`) and using the Select placeholder/registered item labels for display.
- [student menu profile placement] Student accounts now access their profile from the left sidebar via a dedicated "My Profile" item that routes to `/profile` and renders the default student profile content. The topbar user dropdown hides the duplicate Profile and Change password actions for students, leaving Sign out available there while non-student accounts keep their existing dropdown actions.
- [student profile hidden tabs correction] Corrected the student profile behavior: the visual tab strip below the hero is hidden, but the section content remains. `/profile` now defaults to the Profile panel below the hero (student identity, guardians, present/permanent address, and enrollment history, all read-only). Student sidebar links keep routing to `/profile?tab=attendance|results|leaves|fees|idcard|tc`, and the profile page reads that query param to render the corresponding content without showing the tab bar. Attendance renders the existing self-attendance panel; Results/Leaves/Tuition fees/ID card/TC render their placeholder panels until those modules are implemented. `npm run typecheck --workspace web`, `npm run lint --workspace web` (0 errors, existing 17 warnings), and `npm run build --workspace web` pass.
- [student homepage advisory] Student accounts now see a student-specific `/` homepage content section instead of the admin dashboard summary: `StudentAdvisoryGuidelines` renders four advisory guideline cards selected in a deterministic shuffled order per student, each with a different author/role. Non-student accounts still render the API-backed `DashboardSummary`. `npm run typecheck --workspace web`, `npm run lint --workspace web` (0 errors, existing 17 warnings), and `npm run build --workspace web` pass.
- [student self-service profile/nav] Student accounts now get a dedicated left-sidebar self-service group: Attendance, Results, Leaves, Tuition Fees, ID Card, and TC. These route into the student profile tab strip (`/profile?tab=...`) instead of exposing staff/admin module routes. The generic Attendance nav item no longer duplicates for students, and query-based sidebar active states are supported under Suspense. `/profile` now branches by role: non-student users keep the existing editable account profile, while student users get a student-profile surface that shows the full student detail cards/enrollment history read-only (`editable={false}`), hero facts for Class / Section / Roll no / Admitted, and only Change photo + Change password actions. Student accounts no longer get the account Edit action, and the topbar Settings menu item is now permission-gated. Frontend supports `/auth/me` returning either `student_id` or `student: { id }`; without that linked student public id, `/profile` shows a clear missing-link state because `GET /students/{id}` is the authoritative full-profile endpoint. `npm run typecheck --workspace web` clean; `npm run lint --workspace web` has 0 errors with the existing 17 warnings; `npm run build --workspace web` passes.
- [student self-profile API contract fix] Follow-up root cause fix for the student self-service profile: the backend `~/Herd/app-api` `UserResource` now includes `branch` (`{id,name}`), `student_id`, and `student` (`{id,name}`) when those relations are loaded, and `AuthController` loads `branch`, `media`, and `student` for login, `/auth/me`, profile update, and photo update responses. This gives the frontend the linked student public id needed to fetch `GET /students/{id}`, so the `/profile?tab=...` sidebar links now switch tab content and the profile hero/cards can render class, section, roll no, branch, student details, addresses, and enrollment history. Added backend coverage in `AuthTest::test_me_returns_linked_student_reference_for_student_accounts`; adjusted stale auth response assertions to the existing public-id contract. `php artisan test --filter=AuthTest` passes (23 tests, 98 assertions); Pint passed on touched API files; frontend `typecheck`, `lint` (0 errors, existing 17 warnings), and `build` pass.
- [auth reset password input fix] Reset-password new-password fields now bypass the Base UI input wrapper and render native password inputs with the same styling. The visible input value is owned by local React state, then validated directly with the existing Zod schema on submit; RHF remains responsible for labels, focus refs, and inline field messages. This removes the fragile RHF/Base UI controlled-value path that repeatedly caused either blocked typing or stale validation. Both password fields include independent show/hide icon buttons. `npm run typecheck --workspace web` is clean; `npm run lint --workspace web` has 0 errors with the existing 17 warnings; `npm run build --workspace web` passes after allowing network access for Next font fetching.
- [resend credentials: students + parents + email/phone login] **Resend login credentials** now exists for students and parents (it already existed for teachers, task 2.4), and the credentials email now shows **both** email and phone as valid logins. **Backend** (`~/Herd/app-api`): two new endpoints mirroring the teacher pattern — `POST /students/{student}/resend-credentials` (`StudentController@resendCredentials`, `permission:student.create`, named `students.resend-credentials`) → `StudentService::resendCredentials()` and `POST /parents/{parent}/resend-credentials` (`ParentController@resendCredentials`, gated by the parents group's `permission:parent.manage`, named `parents.resend-credentials`) → `ParentService::resendCredentials()`. Each regenerates the linked user's password (`Str::password(10)`), revokes all tokens, and dispatches `SendCredentials` (`afterCommit`) with the matching role label. Guards: students must be `StudentStatus::Active` (else **409**); both abort **422** when the linked user has **no email** on file (credentials are email-only, so there'd be no delivery channel) — the frontend confirm copy warns about this. Route binding is `public_id` for both `Student` and `ParentProfile` (the latter uses `HasPublicId`; the pre-existing parent link/unlink tests that address `$parent->id` remain the known integer-vs-public-id route-key debt, untouched). **Login-identifier change**: `SendCredentials` no longer derives a single role-based `identifier`; it passes the user's `email` **and** `phone` to `CredentialsMail` (both nullable), and `resources/views/mail/credentials.blade.php` renders a "Login (email)" and/or "Login (phone)" line for whichever is present (login already accepts either via `AuthService::login`). `CredentialsMail`'s `identifier` param became `email` + `phone`; the `loginIdentifier()` helper was removed. **Tests** updated/added (all green; Pint clean): `TeacherCredentialsTest` and `StudentEndpointsTest` now assert `$mail->email`/`$mail->phone` instead of `identifier`; new resend tests in `StudentEndpointsTest` (queues mail for active student, 422 no-email, 409 inactive, 403 without `student.create`) and `ParentEndpointsTest` (queues mail, 422 no-email, 403 without `parent.manage`). **Frontend** (`apps/web`): `useResendStudentCredentials` / `useResendParentCredentials` (`POST …/resend-credentials`, barrel-exported). Student kebab `student-row-actions.tsx` gains a `student.create`-gated "Resend credentials" item; student-detail hero `⋮` menu gains the same (gated `STUDENT_CREATE`). Parent `parent-row-actions.tsx` gains a "Resend credentials" item (the whole parents module is already `parent.manage`-gated at the route) and parent-detail hero gains a secondary "Resend credentials" button. All four surfaces (student list + detail, parent list + detail) confirm via the shared `ConfirmDialog` (`@/components/teachers/confirm-dialog`) before mutating, with success/error toasts; the confirm copy notes an email is required. `npm run typecheck --workspace web` clean; `npm run lint --workspace web` 0 errors (only the pre-existing 17 warnings); `npm run build` green.
- [2.7 student/user email correction] Student email now follows the same user-owned identity model as parents and teachers: `student_email` is read from and written to the student's linked `users.email`; no `father_email` / `mother_email` columns or student-resource attributes exist. The previously attempted guardian-email fields were removed from backend requests/resources/models/tests and from all student create/edit/detail frontend payloads. Direct student create still accepts optional `student_email`, and the optional linked parent account still accepts `parent_email` because that belongs to the parent user's login. `UpdateStudentRequest` validates nullable unique `student_email` against `users.email`, and `StudentService::update()` updates the linked user in the same transaction as the student profile. Backend create validation also rejects matching student/parent emails case-insensitively. Focused backend tests for create/update email pass; Pint passed in the API repo; `npm run typecheck --workspace web` is clean; `npm run lint --workspace web` has 0 errors with the existing warning set; `npm run build` passes after allowing network access for Next font fetching (Turbo emitted a post-build no-space-left cache warning after the successful task).
- [2.4 teacher/user email correction] Teacher email now follows the same user-owned identity model: `TeacherResource` reads email from the loaded `user`, teacher listing eager-loads `user`, search checks `users.email`, and `UpdateTeacherRequest` validates email against `users.email` while `TeacherService::update()` writes the linked user in the same transaction. The legacy `teachers.email` column is still mirrored for compatibility until a later schema cleanup removes it. Teacher edit modal and inline profile card now include email in `PUT /teachers/{id}`. Focused backend tests for teacher list search/update/duplicate email pass; Pint passed in the API repo; `npm run typecheck --workspace web`, `npm run lint --workspace web`, and `npm run build` pass with the existing lint warnings and the same post-build Turbo cache space warning.
- [2.7 direct create emails] Student direct-create now exposes optional email fields for both the student login and the linked parent login: `student_email` in the Student section and `parent_email` when "Create a parent account" is enabled. The RHF/Zod schema validates email format/length, prevents using the same email for both logins, clears the hidden parent email when the parent account box is unticked, maps backend `422`s for `student_email`/`parent_email`, and sends the new fields in `StudentCreateInput` (`POST /students`) while preserving the existing phone-first login flow. `npm run typecheck --workspace web` is clean; `npm run lint --workspace web` has 0 errors with the existing warning set; `npm run build` passes after allowing network access for Next font fetching.
- [2.8 parent email] Standalone parent creation already carried `email`; the create dialog now treats it as an actual email input (`type=email`, email input mode/autocomplete) and mirrors the backend `max:150` validation before sending `ParentCreateInput.email` to `POST /parents`. `npm run typecheck --workspace web` is clean; `npm run lint --workspace web` has 0 errors with the existing warning set; `npm run build` passes after allowing network access for Next font fetching.
- [3.3] Teacher attendance is implemented on the shared `/attendance` route. Teachers (role-gated self-service) now get a "My attendance" surface with today's server-sourced status, check-in/check-out actions (`POST /teacher-attendance/check-in` and `/check-out`), month/year history from `GET /me/teacher-attendance`, API summary tiles, loading/empty/error states, and inline surfacing of the API's check-in rejection reason (including IP-whitelist 403s). Staff with `teacher_attendance.view`/`teacher_attendance.manage` now get a Teacher records tab: paginated `GET /teacher-attendance` table/card list, teacher/date/status filters, branch-aware cache keys via the existing branch context/interceptor, and a Zod/RHF correction dialog that confirms before `PUT /teacher-attendance/{id}`, maps 422s to fields, shows success/error toasts, and invalidates the teacher-attendance cache. The Attendance nav/search item now also surfaces for teacher self-service and teacher-attendance permissions. `npm run typecheck --workspace web` is clean; `npm run lint --workspace web` has 0 errors with the existing warning set only; `npm run build` passes after allowing network access for Next font fetching.
- [3.3 design] The teacher "My attendance" surface (`components/teacher-attendance/teacher-checkin-widget.tsx`) was rebuilt to the imported "Teacher Attendance" Claude design, adapted onto the project design tokens (no raw hex). Today's status is now a two-column **hero card**: left side carries the calendar date with a live `Today · …` clock pill (ticks once/sec while checked in), the branch-network shield notice, and Status/Check-in/Check-out facts; the right **timer panel** shows a `Working now` pulse + a live `HH:MM:SS` elapsed timer counting from `check_in_at`, switching to a static "Total worked today" once checked out. The action button cycles Check in → Check out (error-toned outline) → Checked out (disabled), all on the existing `useCheckIn`/`useCheckOut` mutations with the API rejection reason still surfaced verbatim inline. The history section is now a `340px + 1fr` grid: an **SVG donut** of the month's present/late/absent/leave breakdown (state-token arc colours, present-share centre %, legend with counts) beside the four colour-tinted **summary tiles** and the records table, which gains a derived **Worked** column (live for today's open record) and an `Active` badge for the open check-out. Mobile collapses to a stacked card list. Data/hooks/API contracts are unchanged. The hero action button is now a fixed-footprint 44px pill (`ACTION_BTN`, `min-w-[156px]`) so Check in / Check out / Checked out never reflow the timer panel, and the `/attendance` workspace tabs ("My attendance" / "Student attendance") plus the nested `AttendanceHub` tabs were restyled to the design's segmented-pill control (`bg-subtle` track, `data-active` surface tab + shadow). `npm run typecheck --workspace web` clean; `eslint` on the changed files is clean.
- [2.7 direct create] **Add new student directly** — a `New student` button on `/students` (gated by the new `student.create` permission via `<Can>`) opens `StudentCreateDialog`, an office path that mints a student without an admission application. This required a **new backend endpoint** (`~/Herd/app-api`). **Backend**: `POST /students` (`StudentController@store`, `permission:student.create`, named `students.store`) → `StudentService::create($data, $branchId)`, which mirrors `AdmissionService::approve` minus the application — one transaction creating the student login (role `student`, phone = father_mobile, random password, email null), the student row (admission_no auto-generated per branch+year via `AdmissionNoGenerator` when blank), the active `Enrollment`, and — when `create_parent_account` — a linked parent login + `ParentProfile` + `parent_student` row; `SendCredentials` is dispatched `afterCommit` for student (and parent). New `StoreStudentRequest` owns the 422s: the full mutable profile (birth_reg_no **required + unique** here, unlike the `sometimes` update rule), the enrollment (`session_id` exists; `class_id`/`section_id` validated branch-scoped in `after()`; `roll_no` 1–65535 unique within session+class+section), optional `admission_no` (unique), `create_parent_account`+`parent_relation`, and `branch_id` (super-admin-only via `targetBranchId()`, the teacher-create pattern). The `*_id` fields arrive as public-id hashes and are resolved to integer keys by `ResolvePublicIds` before validation. New `student.create` permission added to `PermissionSeeder` and the **admin** role in `RoleSeeder`. 5 new feature tests in `StudentEndpointsTest` (creates login+enrollment, parent link+credentials, duplicate-roll 422, section-outside-class 422, 403 without permission) — all green; Pint clean. (The 6 pre-existing red tests in that file are the known integer-`$student->id`-vs-public-id-route-key debt, untouched by this change.) **Frontend**: `types/student.ts` adds `StudentCreateInput`; `useCreateStudent` (`POST /students`, invalidates `["students"]`); `components/students/student-create-dialog.tsx` — RHF+Zod dialog reusing the profile-field layout from `StudentFormDialog` plus the enrollment/parent fields from the admissions `ApproveDialog` (`SessionSelect`/`ClassSelect`/`SectionSelect`[class-scoped, cleared on class change]/roll/optional admission no/parent checkbox+relation), with `BranchSelect` shown for super admins; `422` → `applyFieldErrors` + `FormBanner`, success toast + close, no double submit. `STUDENT_CREATE` slug exported from `permissions.ts`/barrels. `tsc` clean, `eslint` 0 errors (the new `form.watch` warning matches the established `ApproveDialog` pattern), `npm run build` green. **Address fields follow the admission `StepAddress` system across every student write surface** (create dialog, list-row edit `StudentFormDialog`, and the inline detail cards): each of present/permanent is a cascading Division → District → Upazila → Post office set plus a free-text village/street, served by a new **shared generic `components/students/address-fieldset.tsx`** (`AddressFieldset<T extends FieldValues>`, built on the admission `LocationSelect` + `lib/geo` BD dataset, each level disabled/reset by its parent, postcode hint) so the cascade lives in one place. The create + edit dialogs add a UI-only `permanent_same_as_present` checkbox that mirrors the present address into the permanent one (kept in sync via effect) and disables the permanent block (the edit dialog pre-ticks it when the stored addresses already match via `permanentMatchesPresent`); the flag is stripped from the API payload. On the detail page the present/permanent addresses stay as **two separate cards** (`StudentPresentAddressCard` / `StudentPermanentAddressCard`) but are wrapped in `StudentAddressCards`, a DOM-less provider holding a shared `AddressMirrorContext`. The present card **publishes its live values** while being edited; the permanent card's "Same as present" checkbox mirrors that context (live present values when the present card is open, else the last-saved present address) into the permanent fields via an effect and locks them while ticked — fixing the earlier one-shot cross-card copy that failed to populate. The permanent card pre-ticks when the saved addresses already match. Each card saves its own block via the full `PUT`. `tsc` clean, `eslint` 0 errors (only the established setState-in-effect / `form.watch` warnings), `npm run build` green.
- [5.2 line items] **Invoices now carry multiple described line items** (e.g. Tuition fee, Admission fee, Exam fee) instead of a single amount, matching the imported invoice design. An invoice's `amount` is the **server-computed sum** of its items (bc math, never a float). **Backend** (`~/Herd/app-api`): new `invoice_items` table (`invoice_id` cascade FK, `description`, `amount`, `sort`) + a **backfill** seeding one "Monthly fee — <period>" item per existing invoice from its stored amount, so every invoice has ≥1 item; new `InvoiceItem` model + `Invoice::items()` hasMany + `InvoiceItemFactory` (and `InvoiceFactory` `afterCreating` attaches one item mirroring the amount, keeping the sum-invariant in tests). `StoreInvoiceRequest`/`UpdateInvoiceRequest` swap the single `amount` for `items[]` (`items.*.description` required ≤255, `items.*.amount` required decimal:0,2; create requires `min:1`, update replaces the list wholesale). `InvoiceService::create`/`update` compute the total from the items, persist them in a transaction, and re-derive status from paid-vs-total on edit; `generate()` writes one item per bulk-generated invoice using the **named fee**'s name + copied amount (re-reading ids after the chunked insert). `InvoiceResource` exposes `items` (detail read only, via `whenLoaded`); detail reads eager-load items in `sort` order. Tests updated (Crud/Generation/Reads) — 26/26 green (the 20 red Payment/Settings/TC tests are **pre-existing** 404s on the clean tree, unrelated). **Frontend** (`apps/web`): `types/invoice.ts` adds `InvoiceItem`, `items?` on `Invoice`, and `items` on the create/update inputs (dropping the single `amount`); new `sumMoney` cents-based helper in `lib/format`. `invoice-form-dialog.tsx` replaces the amount input with a **dynamic add/remove line-item list** (`useFieldArray`, description + amount rows, Trash to remove, "Add item", a live **Total**); in edit mode it fetches the invoice **detail** (`useInvoice`) so the existing items seed the rows instead of collapsing to one line. `invoice-paper.tsx` renders the item rows (with a single-line fallback for an item-less invoice). `tsc` clean, `eslint` 0 errors (only the standard RHF `watch()` compiler note every dialog carries).
- [2.7 inline edit] Student detail Profile tab now edits **inline per card** instead of via the hero Edit modal. The hero **Edit button was removed** and **Change photo + Deactivate/Activate now both live in the hero `⋮` overflow menu** (no inline primary action); the `StudentFormDialog` stays for the list-row edit path only. New `components/students/student-profile-cards.tsx` adds a shared `InlineEditCard` (exported as `StudentIdentityCard`/`StudentGuardiansCard`/`StudentPresentAddressCard`/`StudentPermanentAddressCard`): a `Pencil` action in each `DetailCard` header (gated `student.update` && non-TC, the same rule as the hero manage actions) flips the card from read-only `DetailRow`s to an inline RHF+Zod form (label-above-input rows), with **Cancel + Save** in the card footer. Saving merges the card's edits over the full mutable set and sends the complete `PUT /students/{id}` payload (so editing one card never drops another's values); `422` → per-field errors via `applyFieldErrors` + a `FormBanner` fallback; success → toast + back to the row view. Per-card Zod subsets are picked from a shared `FIELD_SCHEMAS` map mirroring `UpdateStudentRequest`; admission_no / admitted render as immutable static rows. **birth_reg_no is now editable** (inline on the Student card) — this needed a backend change: `UpdateStudentRequest` drops its `prohibited` rule for `birth_reg_no` in favour of `['sometimes','required','string','max:25', Rule::unique('students','birth_reg_no')->ignore($this->route('student'))]` (admission_no stays prohibited), with two new tests (change + uniqueness, green). `StudentUpdateInput` gained `birth_reg_no`; the list `StudentFormDialog` keeps it read-only and resends the current value unchanged (the `sometimes`/ignore-self rule makes that a no-op). Shared kit: `DetailCard`/`DetailCardHeader` gained an optional `action` slot (right-aligned header control). `tsc` + `eslint` + Pint clean.
- [2.7 enrollment edit] **Enrollment history rows are now inline-editable** (session/class/section/roll/status), which required a **new backend endpoint** (`~/Herd/app-api`). **Backend**: `PUT /students/{student}/enrollments/{enrollment}` (`StudentController@updateEnrollment`, `permission:student.update`, named `students.enrollments.update`); the `{student}` binding is branch-scoped (out-of-branch → 404) and the controller confirms `enrollment->student_id === student->id` (else 404) since `Enrollment` carries no local branch. New `UpdateEnrollmentRequest` owns the 422s (session exists; class in-branch; section belongs to class; roll 1–65535; status = `EnrollmentStatus`; **duplicate-roll** guard excludes the row being edited via `whereKeyNot`). `StudentService::updateEnrollment` updates + eager-loads session/class/section. `EnrollmentResource` now also returns `session_id`/`class_id`/`section_id` (public ids) so the client can prefill the academic selects. 4 new feature tests in `StudentEndpointsTest` (happy path, section-outside-class 422, duplicate-roll 422, other-student 404) — all green; Pint clean. (Note: the file's *other* pre-existing tests stay red — they address students by integer `$student->id` against the `public_id` route key, the known public-id route-key debt; the new tests use `->public_id`.) **Frontend**: `types/student.ts` adds `EnrollmentStatusValue` + the `*_id`s on `StudentEnrollment` + `EnrollmentUpdateInput`; `useUpdateEnrollment` (invalidates `["students"]`); `student-enrollments.tsx` gains an `editable` prop — a per-row `Pencil` swaps the table row (or mobile card) for an inline `EnrollmentEditor` (`SessionSelect`/`ClassSelect`/`SectionSelect`[class-scoped, cleared on class change]/roll number/`AcademicSelect` status) with Cancel/Save, client-required validation + `422` field mapping + `FormBanner`, success toast. `student-detail.tsx` passes `editable` (same `student.update` && non-TC gate). `tsc` + `eslint` clean.
- [3.2] Attendance **read sheets** — class monthly, per-student monthly, student self-view, and parent child-switch — all served from the single `/attendance` route, now role/permission/param-aware (`app/(app)/attendance/page.tsx`, wrapped in `Suspense` for `useSearchParams`): `?student={id}` → per-student sheet (reached from student-detail + parent my-students links; the API authorizes staff/self/linked-parent and hides denials as 404, so the route doesn't hard-gate); staff with `attendance.create`/`attendance.view` → the **AttendanceHub** tabs (Entry [3.1] + Class sheet), collapsing to a single surface when only one permission is held; a `parent` role → **ParentAttendance** (child selector over `GET /me/students` → `GET /students/{id}/attendance`); a `student` role → **SelfAttendance** (`GET /me/attendance`). Implemented against the real backend (`~/Herd/app-api` `AttendanceController` + `MonthlyAttendanceResource`/`AttendanceResource` + `ListAttendanceRequest`/`MonthlyAttendanceRequest`). **Data layer** (`hooks/attendance`, barrel-exported): `useClassAttendanceMonth` assembles the class pivot from `GET /attendance` — the browse endpoint has no month filter (only exact `date`) and is ordered `date desc`, so it pages (per_page=100) keeping rows inside the month and stops once a page's oldest row predates the month, capped at `MAX_PAGES=60` (surfaces `truncated`); `useStudentMonthlyAttendance(id,{month,year})` and `useMyMonthlyAttendance({month,year})` read the pre-aggregated monthly resource (`{month,year,summary:{present,absent,late,leave,working_days},days:[{date,status}]}`). **No totals are recomputed client-side** — the pivot is a pure reshape and the summary tiles render the API's counts verbatim (`feature-specs/10`, Rules). New `lib/attendance/month.ts` holds 1-based-month calendar helpers (days/firstWeekday/iso/label/prev/next/isCurrentOrFuture). **Components** (`components/attendance`): `MonthlySheetView` (summary tiles + a tinted calendar grid + legend, shared by the per-student/self/parent views), `MonthlySheetFrame` (month-nav header + loading/error/not-found[404]/empty wiring), `MonthNav` (prev/next, Next disabled past the current month), `ClassAttendanceSheet` (class+section selectors, month nav, sticky-first-column pivot table with P/A/L/Lv status cells + legend + truncation banner), `StudentAttendanceSheet` (back button), `SelfAttendance` (403→access state), `ParentAttendance` (derives the effective child from the pick → first linked, no effect). New types in `types/attendance.ts` (`AttendanceListParams`, `MonthlyAttendance`/`*Summary`/`*Day`/`*Params`, `ClassAttendanceRow`/`ClassAttendanceMonth`). **Nav**: `NavItem` gained an optional `roles?` (the documented self-service exception — students/parents hold no permissions, so a route they must reach can only be surfaced by role); the Attendance item now carries `roles:["student","parent"]`, and both `sidebar-nav.tsx` and `global-search.tsx` filters honor permission-OR-role. All four states present across every surface; `npm run typecheck`, `lint` (only the pre-existing six warnings; no new ones), and `npm run build` pass. **Note**: the class sheet only shows students with ≥1 mark in the month (the browse endpoint exposes no roster), and class-sheet rows don't link to the per-student sheet because `AttendanceResource` carries no student public id (only `enrollment_id`); the per-student sheet is reached from the student-detail/parent links instead.
- [3.2 design] `ClassAttendanceSheet` re-skinned to the imported "Monthly Attendance" design (Claude Design `Attendance Sheet (shadcn)`): a page header ("Monthly Attendance" + class/section·month subtitle + an **Export sheet** button that downloads the visible grid as CSV), a single toolbar card (class/section selectors + a new `MonthYearPicker` — prev/next arrows flanking **Month** and **Year** dropdowns so a distant month is one click away; future months/years disabled, and picking the current year clamps a future month back — with a P/A/L/E + Holiday legend), and the pivot table refit — day columns now show the day number over a weekday letter with weekend (Fri/Sat) and today column tinting; the leading Student column pins an avatar (initials) + name + roll; a new sticky trailing **Att %** column shows attended (present+late) ÷ marked days, colour-graded (≥90 success / ≥75 warning / else error); status cells are 24×24 tonal badges (P/A/L/E) and unmarked weekend days render a muted "·". The % is the only derived figure and has no API total to defer to (the grid is still a pure client-side reshape of `GET /attendance`). New `lib/attendance/month.ts` helpers `weekdayOf`/`isWeekend`/`isToday`. `tsc`, `lint`, and `npm run build` pass.
- [3.1 links + user profile] Attendance roster rows are now linked: the student name → `/students/[id]` (gated `student.view`), and "recorded by" → the recorder's teacher detail page when they're a teacher (`teachers.view`) or a new read-only user profile page `/users/[id]` otherwise (gated `role.manage`); without the relevant permission the name renders as plain text. Backend (`app-api`): `AttendanceSheetResource` now returns `student_id`, `recorded_by_teacher_id`, and `recorded_by_user_id` (service eager-loads `recorder.teacher`); added `GET /users/{user}` (`UserController@show`, `role.manage`) returning `UserAccountResource`, with two new `AccessControlTest` cases (the other pre-existing failures in that file are unrelated public-id route-key debt). Frontend: `types/user.ts`, `useUser`, `components/users` (`UserProfileView`, `USER_VIEW`), and `app/(app)/users/[id]`. Both the own-profile (`/profile`) and the new user profile use the shared `detail-ui` layout (`DetailHero`/`DetailCard`/`DetailRow`) so they match the teacher/student detail pages. Verified `tsc` clean and `eslint` no errors.
- [3.1 UX] Attendance entry refinements: client-side roster pagination (10/page via shared `ListPager`); status buttons toggle off when re-clicked; selection (class/section/date) and unsaved marks persist to `localStorage` so a refresh re-opens the roster with work intact; partial/incremental saves send only marked students (backend upserts by enrollment+date, so repeated saves merge without duplicates); specific validation banner messages instead of a generic "complete the fields"; and **autosave** — after `AUTOSAVE_DELAY_MS` (3s) idle, unsaved marks (those differing from the server baseline) are pushed automatically, with an under-button indicator (Saving… / Unsaved — autosaving… / All changes saved / Autosave failed). Verified with `tsc --noEmit -p apps/web` (clean) and `eslint` (no errors; only the standing React Compiler `set-state-in-effect`/css-conflict warnings).
- [3.1 fix v2 / public-id select migration] Root cause of "can't select class" was that backend resources now expose opaque `public_id` **hash strings** as their `id` (see `app-api` `ClassResource`/`SectionResource`/etc.), but the shared `AcademicSelect` coerced every option value with `Number()`, turning a hash into `NaN` and silently clearing the selection — across attendance, the students/admissions filters, and teacher assignments. The earlier numeric-coercion fix was therefore wrong and has been reverted. `AcademicSelect` is now generic over the id type (`AcademicSelect<T extends string | number>`); it matches options by string form and emits the option's original value untouched (no coercion). The typed wrappers (`ClassSelect`/`SectionSelect`/`SubjectSelect`/`SessionSelect`/`BranchSelect`/`TeacherSelect`) expose `AcademicSelectProps<string>`. Entity id fields (`SchoolClass`/`Section`/`Subject`/`AcademicSession`/`Teacher`/`Branch` + their FKs and the academic/teacher `*Input` payloads + attendance/admission select-driven params) are now `string`, mutation hooks take `id: string`, and the active-branch bridge/provider store a `string | null` branch id. The backend's global `ResolvePublicIds` middleware resolves these hashes back to integer keys on every request, so sending the `public_id` is correct. The public admission wizard is intentionally left numeric (its `GET /public/settings` returns raw numeric ids). Verified with `tsc --noEmit -p apps/web` (clean) and `eslint` (pre-existing warnings only, no errors).
- [3.1] Student attendance entry shipped at `/attendance`, gated on the real backend `attendance.create` permission because `GET /attendance/sheet` and `POST /attendance` are both create-gated. Added `types/attendance.ts` for the real roster/bulk-save contracts; `hooks/attendance` reads `GET /attendance/sheet` with class/section/date params and bulk-upserts via `POST /attendance`, plus a `PUT /attendance/{id}` hook for the ticket's single-record correction endpoint. The entry UI (`components/attendance/student-attendance-entry.tsx`) uses the shared `ClassSelect`/`SectionSelect`, a date field, RHF+Zod validation, roster table/card views, per-student segmented controls for present/absent/late/leave, "Mark all present", status counts, per-row `422` mapping (`records.N.enrollment_id/status`), loading/empty/error/loaded states, and success/error toasts. Existing marks from the sheet prefill the controls, and re-submitting the same date relies on the backend's idempotent upsert; no client-side once-per-day or TC filtering was added. Sidebar Attendance now requires `attendance.create` for this entry route; task 3.2 should revisit route/navigation behavior when read-only attendance sheets are implemented. Verified with `npm run typecheck --workspace web`, `npm run lint --workspace web` (only the pre-existing six warnings), and `npm run build --workspace web` (network escalation required for configured `next/font` Google font fetches).
- [2.8 UI refinement] Parent detail layout simplified per feedback: `/parents/[id]` now renders Parent information as one full-width card with compact responsive info tiles, followed by a separate full-width Linked students card below it. Removed the previous side-by-side parent/students layout so the page reads top-to-bottom and the linked-student table has the full content width. `npm run typecheck --workspace web`, `npm run lint --workspace web` (existing six warnings only), and `npm run build` pass.
- [2.8 refinement] Parents list design fixed: `/parents` now keeps each row compact and scannable — the Linked students column shows only a count badge plus a short name preview, and the full linked-student management was moved out of the table cell. Parent rows and the row action menu now open `/parents/[id]`. Added a parent detail management route (`app/(app)/parents/[id]/{page,loading}.tsx`) and `ParentDetail` (`components/parents/parent-detail.tsx`): status-accented detail hero, parent information card, and a responsive linked-students table/card list with student profile links, attendance/results/invoices quick links, `Add link`, and unlink confirmation. Added `useParent(id)` (`hooks/parents/use-parent.ts`) which scans existing `GET /parents` pages for the requested public id because the inspected backend has no `GET /parents/{id}` endpoint; swap to a direct detail endpoint if the API adds one. `npm run typecheck --workspace web`, `npm run lint --workspace web` (only the pre-existing six warnings outside this module), and `npm run build` pass.
- [2.8] Parents module: admin management at `/parents` plus parent self-service at `/my-students`, each with route-level `loading.tsx` + `error.tsx`. **Implemented against the real backend** (`~/Herd/app-api` `ParentController`, `ParentResource`, `LinkedStudentResource`, `StoreParentRequest`, `LinkStudentRequest`, `ParentService`): admin endpoints are gated on the single backend slug **`parent.manage`**, while `/me/students` is API role-gated because parents hold no staff permissions. New `types/parent.ts` models `ParentProfile`, `LinkedStudent`, `ParentRelation`, create/link inputs, and label helpers. New hooks in `hooks/parents`: `useParents` reads `GET /parents?search=&page=&per_page=15` with `requestPaginated`, branch folded into the key, `keepPreviousData`, and `STANDARD` stale; `useCreateParent` posts `{name, phone, email?, relation, student_ids}`; `useLinkParentStudent` posts `{student_id}` to `/parents/{parent}/students`; `useUnlinkParentStudent` deletes `/parents/{parent}/students/{student}`; `useMyStudents` reads `GET /me/students`. Mutations invalidate `["parents"]` and `["students"]` so linked-student and student surfaces refetch. **Admin list** (`parents-list.tsx`): search by name/phone, paginated responsive table/card list (Parent · Contact · Linked students · Actions), create button, linked-student count, linked student chips/cards with profile links, unlink confirmation, and link-students dialog. Four states are present (loading/empty/error/loaded), with filter-aware empty copy. **Create parent** (`parent-form-dialog.tsx`): RHF+Zod for name/phone/email/relation/student_ids, field-level `422` via `applyFieldErrors`, success toast with the backend credential-dispatch copy ("Credentials are being sent"), and no client-side credential generation. **Student linking** (`link-students-dialog.tsx`): reuses `StudentPicker`, excludes already-linked students, supports selecting multiple students and calls the single-student backend link endpoint sequentially; duplicate-link `409` and validation failures surface via toast/banner. **My students** (`my-students-list.tsx`): `GET /me/students` linked children only, with loading/empty/error/access states; cards link to attendance (`/attendance?student=`), results (`/results?student=`), and invoices (`/fees?student=`). Added a `Parents` sidebar item under People gated by `parent.manage`; no role-name UI gate was added. `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean with only the pre-existing six React-Compiler warnings outside this module; `npm run build` passes (needed network escalation for configured `next/font` Google font fetches).
- [2.7] Students module: full management at `/students` (list) and `/students/[id]` (detail), each with `loading.tsx`, plus a shared `students/error.tsx` boundary. **Implemented against the real backend** (`~/Herd/app-api` `StudentController` + `StudentListResource`/`StudentResource`/`EnrollmentResource` + the `Student/*` form requests + `StudentStatus` enum + `StudentPolicy` + `StudentService`), not assumptions. **Resource ids are the model `public_id`** (an opaque hash string), so `types/student.ts` types a student id as `string` throughout. **Status model** = the `StudentStatus` enum exactly: `active` / `inactive` / `tc`. **Permission slugs match the backend**: `student.view` (list) and a single `student.update` (every write — profile update, status flip, photo). Corrected the 1.5 nav + 1.6 search slug from the assumed `students.view` → real `student.view`; also widened the 2.4 teacher mutation hooks' `id` params to `string | number` (a pre-existing build break from the hash-id migration commit `7cc0dfd` — `teacher-detail.tsx` passed the string route id to hooks typed `number`; build was red at HEAD). **List** (`students-list.tsx`) reads `GET /students?search=&status=&class_id=&section_id=&session_id=&page=&per_page=15` via `useStudents` (`requestPaginated`, `keepPreviousData`, branch folded into the key, `STANDARD` stale); debounced search (name_en/name_bn/admission_no/father_mobile server-side), the 2.1 `ClassSelect`/`SectionSelect`(class-scoped)/`SessionSelect` filters + a 4-state status filter (All/Active/Inactive/TC), and "Clear filters". Responsive table (Student avatar+name EN/BN · Admission no · Class/Section · Roll · Status · Actions) / card-list; **no branch column** — the list resource carries none (branch auto-scoped by `BranchScope`). Status badge uses the shared `statusTone` (active→success, inactive→error, **tc→info** with a "TC" label). Four states (loading/empty/error/loaded; empty copy is filter-aware). Edit/photo dialogs open from a list row via thin loaders that fetch the full profile by id (rows are compact). **Detail** (`student-detail.tsx`, `GET /students/{id}` → `StudentResource`): a **TC info banner** when `status==='tc'`, header (photo + name EN/BN + status badge + manage actions), then Student / Guardians / Present address / Permanent address field grids, and an **Enrollment history** section (`student-enrollments.tsx`, the dedicated `GET /students/{id}/enrollments` → `EnrollmentResource`, its own loading/empty/error states, responsive table/cards). Forward links gated by their slugs: Attendance (`/attendance?student=`, `attendance.view`), Results (`/results?student=`, `result.view`), ID card (`idcard.generate`), TC (`tc.view`). The detail route is **not** client-gated on `student.view` — the API authorizes via `StudentPolicy::view` (staff, the student itself, or a linked parent), so a student/parent opens its own record without the staff list permission; an unauthorized/out-of-branch/missing record surfaces as **not-found** (the API hides it as `404`, never `403`, via `isNotFoundError`). **Edit** (`student-form-dialog.tsx`, RHF+Zod, edit-only — students are created via the 2.6 admissions-approval flow) updates the mutable profile fields via `PUT /students/{id}`; **admission_no + birth_reg_no are shown disabled/read-only and never sent** (the API `prohibited`s them → 422); `422` maps onto fields via `applyFieldErrors` + a banner fallback. **Photo** (`student-photo-dialog.tsx`, adapted from 2.4) multipart `POST /students/{id}/photo` with the shared `ImageDropzone`+`ImageCropper`, jpg/png ≤2MB client validation; **no delete route exists**, so "Remove image" only clears the pending pick. **Status toggle** (`PATCH /students/{id}/status`) via the reused 2.4 `ConfirmDialog`, only active↔inactive — TC is rejected by the backend (`Use the TC module…`) and is hidden client-side for TC students (retired). Writes go through `hooks/students/use-student-mutations.ts`, all invalidating `["students"]`. `tsc` + `npm run build` + `lint` clean (only the pre-existing accent/theme-switcher warnings).
- [2.6] Admissions **review** (authenticated admin surface): the queue at `/admissions` (list) and `/admissions/[id]` (detail), each with `loading.tsx` + the shared `admissions/error.tsx` boundary, mounted **inside** the `(app)` group (server auth guard + app shell). Coexists with the 2.5/2.9 **public** routes (`/admissions/application`, `/admissions/status`, outside `(app)`) — no route collision (public ones nested deeper; `/admissions` had no `page.tsx`). Both routes gate on `admissions.view`. **Implemented against the real backend** (`~/Herd/app-api` `AdmissionController` + `AdmissionListResource`/`AdmissionDetailResource` + `ListAdmissions`/`Approve`/`RejectAdmissionRequest` + `AdmissionStatus` enum + `AdmissionService`), not assumptions. **Status model**: the enum is exactly `pending` / `approved` / `rejected`, and the **list always filters by one status, defaulting to `pending`** — there is no "all". So the queue's status filter offers those three and **defaults to Pending**; switching to Approved/Rejected reveals the rest. (This was the cause of an early "only 8 of 36 show, no pager" report — the 8 were the pending subset; the others are approved/rejected under their own status.) **Queue** (`admissions-list.tsx`) reads `GET /admissions?status=&desired_class_id=&search=&page=&per_page=15` via `useAdmissions` (`requestPaginated`, `keepPreviousData`, branch folded into the key, `SHORT` stale); debounced search (name_en/name_bn/app_no/father_mobile/birth_reg server-side), a **class filter** sent as `desired_class_id` (2.1 `ClassSelect`), the 3-state status filter, and a "Clear" (search+class). Responsive table (Applicant · Application no · Class · Mobile · Submitted · Status · Actions; **no branch column** — the list resource carries none, branch is auto-scoped by `BranchScope`) / card-list; the list resource returns only `{id, application_no, name_en, desired_class{id,name}, father_mobile, status, submitted_at}` so the row avatar is initials-only (no photo on the list). Inline **Approve**/**Reject** per row, only while `pending`, gated by `admissions.manage`. Four states (loading/empty/error/loaded; empty copy is status-aware). **Detail** (`admission-detail.tsx`, `GET /admissions/{id}` → `AdmissionDetailResource`): header (photo via `photoUrl()` w/ `User` fallback, name_en/name_bn, app no, desired class, status badge + approve/reject while pending), error-toned **rejection-reason** panel, then Student (name bn/en, DOB, birth reg, religion, nationality, caste — **no gender**, not in the resource), Admission (app no, desired class, submitted/reviewed dates, reviewed-by), Guardian (father/mother name bn/en + NID + mobile), Address (present/permanent joined village→division), **Previous education** list, and a **Documents** grid (Spatie media `{name,url}`, each a download link). **Approve** (`approve-dialog.tsx`) is the **office-use form** the backend requires (`ApproveAdmissionRequest`), not a bare confirm — RHF+Zod collecting `session_id` (defaults to the current session), `class_id` (defaults to the desired class, overridable) + `section_id` (2.1 selectors, section scoped to class), `roll_no`, optional `admission_no` (auto-generated when blank), and `create_parent_account` + `parent_relation` (father/mother/guardian, required-if). `422` (incl. roll-uniqueness and the branch-scoped class/section `after()` checks) maps onto fields via `applyFieldErrors`; on success a **result phase** shows the created student (`admission_no` + enrollment session/class/section/roll + whether a parent account was created) and a "View student" link — the server generates a random password and **dispatches login credentials** (`SendCredentials` job) to the student (phone = father_mobile) and parent, so nothing is built client-side. **Reject** (`reject-dialog.tsx`): required-reason Textarea → `POST .../reject {rejection_reason}` (the actual field), client + `422`-on-`rejection_reason` validation; the service's already-reviewed **409** surfaces as a toast. Both mutations (`use-admission-mutations.ts`) invalidate **`["admissions"]` + `["students"]`** so a processed application **leaves the pending queue** and the new student appears. New `types/admission.ts` admin types (`Admission`, `AdmissionDocument`, `AdmissionStatusValue`/`Filter`, `AdmissionListParams`, `AdmissionApproveInput`, `AdmissionApproveResponse`, `ParentRelation` + `admissionApplicantName`/`documentLabel`/`documentUrl`/`isPendingAdmission`); class read via the shared `statusClassName` (nested `desired_class`). `admission-tone.ts` maps pending→warning/approved→success/rejected→error. New `components/admissions/review/{index,permissions,media}.ts`; slugs `admissions.view`/`admissions.manage`. `npm run build` + TypeScript pass; `lint` 0 errors (the approve form carries the same standard React-Compiler "incompatible library" notice every RHF dialog in the repo has).
- [2.9 design] The status-check screen was re-implemented to the imported **Claude Design** ("Admission Status Check", project `6abf891c`, owned by the user — connector connected via `/design-login`, design-system scopes granted). The design's hardcoded Tailwind palette is mapped onto the app's design-system tokens (`ui-context.md`, no hardcoded hex): slate-900 → `copy-primary`/`surface`, blue-600 → `brand`, emerald-600 → `success`. **Form**: centered card with a brand mark + eyebrow + title, a blue gradient top-rule, two large fields, and a dark full-width "Check Status" button (combined "enter both" error on submit; the mock's "Demo credentials" hint box was intentionally dropped). **Found**: a dark navy banner (mark + "Admission Application" + Status word + class `Badge`), a **Submitted → Under Review → Decision stepper** (`statusStepIndex` derives the active step from the raw status; done=success/check, active=brand), a Branch/Class/Session card beside a 150px applicant photo box (User-icon fallback), and a "Student" detail card (Name bn/en, DOB, birth reg, religion, nationality, caste), then a "Check Another Application" outline button. **Not found**: red-circle `SearchX` card with "Try Again". The data layer is unchanged from the build below (URL-driven, real `usePublicAdmissionStatus`, Back-preserves-inputs); the **network/5xx retry view** (`ErrorView`, warning-toned card) was kept since the design mock has no failure path. The shared chrome was split: `AdmissionPublicShell` now builds on a lower-level `AdmissionThemeSurface` (font + token re-point + `.admission-fields` CSS + `<main>`), and the status page uses the bare surface with a soft `from-base`→`to-subtle` gradient because the design lays out its own banner/cards rather than a single bordered card. `AdmissionStatus` gained `caste`. **Brand**: "Madani Pathshala" / "M" are top-of-file constants (matching the design); swap when wired to `school_name`/`school_logo`. `tsc` + `npm run build` + `lint` clean.
- [2.9] Public admission application **status check** (standalone, unauthenticated): `/admissions/status` (`app/admissions/status/{page,loading}.tsx`), mounted **outside** the `(app)` group like the 2.5 form, so no server auth guard and no app shell — no token is sent (the route mounts outside the auth/branch providers). Shared chrome was extracted: `components/admissions/public/admission-public-shell.tsx` (`AdmissionPublicShell`) now owns the themed standalone surface — the Libre Franklin font, the `admissionTheme` CSS-var re-point (fixed light navy-on-white palette, pinned regardless of dark mode), the `.admission-fields` 46px field overrides, and the centered header/card/footer — and **both** the 2.5 application page and this status page render through it (the 2.5 page was refactored to drop its inline copies; no behaviour change). The screen (`admission-status-check.tsx`, client) is **URL-driven**: submit commits `?application_no=&date_of_birth=` to the URL via `router.push`, which fires the lookup; because it's a same-route client navigation the component stays mounted, so typed inputs survive — **browser Back** from a result reverts to the lookup form with the inputs preserved (fix a typo without re-typing), while the explicit **Try Again** / **Check another application** buttons clear them and reset the query. **Lookup form**: two required fields (application number text + native `type="date"` DOB), red `*` labels, client-side validation (inline errors on submit attempt), submit disabled until both filled. **Found**: applicant card — prominent rounded `Avatar` (photo via `AvatarImage`, initials fallback), `name_en`/`name_bn`, application no, and a **prominent colour-coded status badge** (`StatusBadge` with an explicit local `STATUS_TONE` map per the ticket enum — `submitted`/`under_review`→info, `approved`→success, `rejected`→error, `payment_pending`/`payment_failed`→**warning**, unknown→neutral; distinct from the generic `statusTone` where `*_failed`→error), plus a `<dl>` grid of DOB (`formatDate`), birth reg no, religion, nationality, branch/class/session **names**. Photo URLs are resolved absolute-vs-relative (relative prefixed with `API_BASE_URL`). **Not found**: a single calm `EmptyState` (does not distinguish DOB-mismatch from no-application — security) with the ticket's exact copy + **Try Again**; triggered by a `404` (`isNotFoundError`) **or** an empty success body. **Network/5xx** are kept separate — an `ErrorPanel` with a retry button, never the not-found screen. New read hook `hooks/admissions/use-public-admission-status.ts` (`usePublicAdmissionStatus({applicationNo, dateOfBirth})`) calls `GET /public/admissions/{no}/status?date_of_birth=YYYY-MM-DD`, enabled only when both present, `SHORT` stale, distinct from 2.5's `useAdmissionStatus` (which has no DOB check); exported from the admissions hooks barrel. `AdmissionStatus` (`types/admission.ts`) gained optional applicant-detail fields (`name_bn`/`name_en`/`date_of_birth`/`birth_reg_no`/`religion`/`nationality`/`session_name`/`photo_url`). Responsive single→two-column detail grid; the client component is wrapped in `Suspense` (`useSearchParams`). `npm run build` + `tsc` pass; `lint` clean. **Assumptions** (contract `docs/api/admissions.md` still absent): the status endpoint accepts `?date_of_birth=YYYY-MM-DD` and returns the same response for both DOB-mismatch and no-application (so we never infer which), with the applicant fields read defensively under the ticket's names; not-found surfaces as a `404`. Adjust the field map when the contract lands.
- [2.5] Public admission form (standalone, unauthenticated): a multi-step wizard at `/admissions/application` (`app/admissions/application/{page,loading}.tsx`) that lives **outside** the `(app)` group, so the server auth guard never runs and no app shell wraps it — a centered, branded light surface (`page.tsx`), using only design tokens. The route page (server component) reads the `?application_no=` search param and hands it to the client `AdmissionWizard` to resume the post-payment flow. **Wizard** (`components/admissions/public/admission-wizard.tsx`): one RHF + Zod form (`schema.ts`) over all fields; each step validates only its own fields via `form.trigger(STEP_FIELDS[step])` before advancing, so forward nav is blocked on invalid data, and the `StepIndicator` allows back navigation to any visited step with data preserved. Seven steps — Branch & Class (`AcademicSelect` reused for both; class list filtered to the chosen branch and reset on branch change), Student Identity (nationality locked to "Bangladeshi"), Guardian, Address (present single-language + permanent bn/en pairs), Previous Education (`useFieldArray`, one optional row by default + add/remove, required-if-filled), Photo & Documents (photo via the shared `ImageDropzone`, jpg/png ≤2MB with preview/remove; up to 5 documents pdf/jpg/png ≤5MB each, inline type/size errors), and a read-only Preview (branch/class by name, photo thumbnail, document filenames; **Edit** returns to step 1 intact). All required fields show a red `*` (`fields.tsx` `Req`/`TextField`). **Submit** builds multipart `FormData` (`build-form-data.ts`, Laravel bracket notation, empty optionals/rows dropped) and `POST`s `/public/admissions`; a `422` maps each error onto its field and **jumps to the first step containing one** (`stepForField`) — duplicate `birth_reg_no` surfaces inline on Student. **Payment** (`payment-flow.tsx`, only when `admission_payment_enabled` + an `invoice_id` came back): always re-fetches authoritative status via `GET /public/admissions/{no}/status` (never trusts redirect params), and renders by phase (`paymentPhaseOf`) — pending → "Proceed to payment" (`POST /invoices/{id}/payments/online` → redirect to the gateway URL), failed/abandoned → "Retry payment" capped at the settings `payment_retry_limit` (default 3, attempts read from the status payload with a sessionStorage fallback that survives the gateway round-trip), exhausted → terminal `payment_failed` with office-follow-up messaging, settled → Confirmation. **Confirmation** (`confirmation-screen.tsx`): `application_no`, status badge (payment path), and a **Download PDF** link to the public application PDF. Payment-disabled path skips straight from submit to confirmation. Hooks in `hooks/admissions/*` (`usePublicSettings`, `useSubmitAdmission`, `useAdmissionStatus`, `useInitiatePayment`) go through the shared `api` client — no token or `branch_id` is attached because the route mounts outside the auth/branch providers (the client's session bridges stay empty). New `types/admission.ts`. Settings/status loading→skeleton, error→`ErrorPanel` (retry); submit/payment failures → field/inline/toast. Responsive single-column→two-column, full-width mobile footer buttons. `npm run build` + `tsc` pass; `lint` clean (only the pre-existing accent/theme-switcher warnings). **Assumptions** (contract `docs/api/admissions.md` + `feature-specs/08` reference it but it's absent): `GET /public/settings` returns `{branches[], classes[] (with branch_id), admission_payment_enabled, payment_retry_limit}`; `POST /public/admissions` (multipart) returns `{application_no, invoice_id?}` and maps `422` by the ticket's field names; the status endpoint returns `{status, payment_status?, payment_attempts?, invoice_id?, pdf_url?}`; the online-payment response carries the gateway URL under one of `redirect_url`/`gateway_url`/`payment_url`/`url`/`GatewayPageURL`; the PDF is `GET /public/admissions/{no}/pdf` (or `status.pdf_url`); SSLCommerz returns the visitor to `/admissions/application?application_no=…`. Adjust the field maps/endpoints when the contract lands.
- [extra] Self-service profile: `/profile` (`app/(app)/profile/*` — `page.tsx` + `loading.tsx` + `error.tsx`), the target the 1.5 user menu already linked to. No permission gate — every authenticated user manages their own profile. `components/profile/profile-view.tsx` reads the resolved `AuthUser` from `useAuth()` (the `AuthProvider` already owns the `GET /auth/me` query and gates the subtree behind its loading/error states, so no extra fetch/states are needed here): a header card (avatar + name + email/username + role badges + Photo / Password / Edit actions), a "Personal information" card (name/email/username/phone), and a read-only "Roles & access" card (branch — "All branches" for super admin — + role badges, with an `EmptyState` note that permissions are admin-managed). **Edit** (`profile-form-dialog.tsx`, RHF + Zod via shadcn `Form`, reuses the academic `form-helpers` `applyFieldErrors`/`FormBanner`) submits `PUT /auth/profile` with `{name, email?, phone?}`; `422` → fields (+ banner fallback), success toast + close, no double submit; re-seeds from the latest user on open. **Photo** (`profile-photo-dialog.tsx`, adapted from the 2.4 teacher photo dialog): multipart `POST /auth/profile/photo` (field `photo`), client-side jpg/png + ≤ 2MB validation with inline error + object-URL preview (revoked on cleanup). **Password** reuses the existing 1.4 `ChangePasswordDialog`. Writes go through `hooks/auth/use-profile-mutations.ts` (`useUpdateProfile`, `useUploadAvatar`) which invalidate the `["auth","me"]` key, so the `AuthProvider` context refetches and the topbar avatar + user menu update too. Added `userInitials(name)` to `types/auth.ts` (shared avatar fallback) and refactored the 1.5 `user-menu.tsx` to use it instead of its private copy; new `ProfileUpdateInput` type. `npm run build` + TypeScript pass; `lint` clean (only the pre-existing warnings). **Assumptions** (`docs/api/auth.md` still absent): self-update is `PUT /auth/profile` returning the updated user envelope; avatar upload is `POST /auth/profile/photo` (multipart `photo`). Adjust the endpoints/field map when the auth contract lands.
- [2.4] Teachers module: full management at `/teachers` (list) and `/teachers/[id]` (detail), each with `loading.tsx` + the shared `teachers/error.tsx` boundary. Both routes gate their own content on `teachers.view` (access-denied `EmptyState` when absent, since the routes are reachable directly); the API stays authoritative. **List** (`components/teachers/teachers-list.tsx`) reads the paginated `GET /teachers` via the new `useTeachers({search,status,page})` hook (`requestPaginated`, `keepPreviousData` for smooth paging, branch folded into the key), with a debounced search box (`useDebouncedValue`, 300 ms), an All/Active/Inactive status filter (`teacher-status-filter.tsx` on the shared `Select`), a "Clear" button, and server-side pagination (`meta`-driven `Pager`; resets to page 1 in the filter change handlers, not an effect). Responsive table (≥ md, with a super-admin-only **Branch** column) / card-list (< md); avatar+identity cell, subjects/classes derived defensively (`teacher-summary.ts` — from explicit `subjects`/`classes` arrays or the `assignments` list), `StatusBadge` for active/inactive; row click → detail. Four states: 8-row `TableSkeleton`, `ErrorPanel` (retry), distinct empty ("no matches" when filtered vs "no teachers yet"), and loaded. **Detail** (`teacher-detail.tsx`) reads `useTeacher(id)` (disabled until id present, `404`/out-of-branch → not-found `EmptyState`), shows profile (contact/employee/gender/address/joined) + assignments cards, an attendance-summary link (`/attendance?teacher={id}`, gated by `attendance.view`), and the manage actions. **Create/edit** (`teacher-form-dialog.tsx`, RHF + Zod via shadcn `Form`, reuses the academic `form-helpers` `applyFieldErrors`/`FormBanner`): personal info + a repeatable class/subject assignment editor (`teacher-assignments-field.tsx`, using the 2.1 `ClassSelect`/`SectionSelect`/`SubjectSelect` scoped per row) + active/inactive; `422` → fields (+ assignment-array errors surfaced), success toast + close, no double submit. On **create**, the success toast surfaces that the API generated and **emailed credentials** (to the returned/submitted email) — passwords are never built or shown client-side. **Photo** (`teacher-photo-dialog.tsx`): multipart `POST /teachers/{id}/photo`, client-side jpg/png + ≤ 2MB validation with inline error and an object-URL preview (derived via `useMemo`, revoked in cleanup). **Status toggle** + **resend credentials** use a shared non-destructive `ConfirmDialog` (confirm-first) → `PATCH /teachers/{id}/status` and `POST /teachers/{id}/resend-credentials`. All writes go through `hooks/teachers/use-teacher-mutations.ts` (create/update/status/photo/resend) which invalidate the broad `["teachers"]` key (refreshes list, detail, **and** the 2.3 `useTeacherOptions` selector). Manage actions are gated by `<Can permission="teachers.manage">` (`components/teachers/permissions.ts` — `TEACHER_VIEW`/`TEACHER_MANAGE`); the kebab `teacher-row-actions.tsx` always offers "View profile" and hides the rest without manage. The 2.3 `teacher-select.tsx`/`use-teacher-options.ts`/`TeacherOption` are folded into the now-full teachers module (barrel `hooks/teachers/index.ts` + `components/teachers/index.ts`); `types/teacher.ts` gained the full `Teacher`/`TeacherInput`/`TeacherAssignmentInput`/summary types + helpers (`isTeacherActive`, `teacherDisplayName`, `teacherInitials`, `namedList`). `npm run build` + TypeScript pass; `lint` clean (only the pre-existing accent/theme-switcher/assignment-dialog warnings). **Assumptions** (contract `docs/api/teachers.md` still absent): list is the standard Laravel paginator (`?search=&status=active|inactive&page=&per_page=15`, envelope `data[]` + `meta`); item/detail fields read defensively (`name`/`full_name`, `is_active` or `status` string, `email`, `phone`, `designation`, `employee_id`, `gender`, `address`, `joining_date`, `photo_url`, `branch_name`, and either `subjects`/`classes` arrays or an `assignments[]` of `{class_id,section_id?,subject_id?,*_name}`); create/update payload is `{name,email,phone?,designation?,employee_id?,gender?,address?,is_active,assignments:[{class_id,section_id?,subject_id?}]}`; status is `PATCH …/status {is_active}`; photo field is `photo` (multipart); permission slugs are `teachers.view`/`teachers.manage`; attendance link target is `/attendance?teacher={id}` (owned by Phase 3). Adjust the field maps / payloads / slugs when the contract lands.

- [2.3] Teacher assignments + branches management: both surfaces live as new tabs on the existing `/academic` screen (`components/academic/management/academic-management.tsx` now has **Sessions / Classes / Assignments**, plus a **Branches** tab rendered only when `useBranch().isSuperAdmin`). **Assignments** (`assignments-manager.tsx`) lists `GET /teacher-assignments` via the new `useTeacherAssignments({teacher_id, class_id, session_id})` read hook (folds the three filters + super-admin `branch` into the key; only set filters are sent), with `teacher_id`/`class_id`/`session_id` filter selectors (shared `ClassSelect`/`SessionSelect` from 2.1 + new `TeacherSelect`) and a "Clear" button; responsive table (≥ md) / card-list (< md); loading/empty (distinct "no matches" when filtered)/error/loaded states. The create/edit dialog (`assignment-form-dialog.tsx`, RHF + Zod via shadcn `Form`) picks teacher + class (required) + section + subject (optional — absent subject = class-teacher, present = subject-teacher); section/subject are the dependent 2.1 selectors scoped to the chosen class and reset when the class changes. **Branches** (`branches-manager.tsx`, super-admin only) lists `GET /branches` through the existing `useBranches` hook with the same responsive table/card layout + four states, and a create/edit dialog (`branch-form-dialog.tsx`) for name (required) + code/address/contact (optional). Both reuse the 2.2 `delete-dialog.tsx` (confirm-first destructive delete), `row-actions.tsx` (kebab menu), and `form-helpers.tsx` (`applyFieldErrors` maps `422` to fields, `FormBanner` for non-field messages); success toasts + close, submit can't double-fire. **Writes** go through new mutation hooks: `hooks/academic/use-teacher-assignment-mutations.ts` (`POST /teacher-assignments`, `PUT/DELETE /teacher-assignments/{id}` → invalidate `["teacher-assignments"]`, re-exported from `hooks/academic/index.ts`) and `hooks/branches/use-branch-mutations.ts` (`POST /branches`, `PUT/DELETE /branches/{id}` → invalidate `["branches"]`, which also refreshes the 1.5 branch switcher's `useBranches` list). Assignment write actions are gated with `<Can permission={ASSIGNMENT_MANAGE}>` (added to `components/academic/management/permissions.ts`); branch CRUD is gated by `isSuperAdmin` (the tab itself is hidden for non-super-admins) — matching how the branch switcher is gated, since there's no documented branch permission. The teacher dropdown source is new and minimal (task 2.4 owns the full teachers module): `types/teacher.ts` (`TeacherOption`), `hooks/teachers/use-teacher-options.ts` (`GET /teachers?per_page=200`, branch-scoped key, REFERENCE stale), and `components/teachers/teacher-select.tsx` (built on the shared `AcademicSelect`). New `TeacherAssignment` type added to `types/academic.ts`. `npm run build` + TypeScript pass; `lint` clean (only the pre-existing accent/theme-switcher warnings). **Assumptions** (contract `docs/api/academic-structure.md` + `docs/api/teachers.md` still absent): assignment write permission is the shared `academic.manage` (`ASSIGNMENT_MANAGE` — split if the contract documents a distinct slug); assignment payload is `{teacher_id, class_id, section_id?, subject_id?}` and the list expands optional `*_name` labels (read defensively with `#id` fallbacks, no session field on the dialog since the ticket lists only teacher+class+section+subject); branch payload is `{name, code?, address?, contact?}`; `GET /teachers` is a paginated list read whole at `per_page=200` for the selector. Adjust when the contracts land.

- [2.2] Academic structure management: the `/academic` screen (`app/(app)/academic/*` — `page.tsx` + `loading.tsx` + `error.tsx`) is the settings-area surface for sessions, classes, sections, and subjects. The page gates its own content on `academic.view` (renders an access-denied `EmptyState` when absent, since the route is reachable directly) and mounts `components/academic/management/academic-management.tsx`, a two-tab (`Sessions` / `Classes`) container. **Sessions** (`sessions-manager.tsx`) lists via the 2.1 `useSessions` hook with a responsive table (≥ md) / card-list (< md), a "Current" `StatusBadge`, and a date-range column. **Classes** (`classes-manager.tsx`) is an accordion list (cards at every width); expanding a class mounts `class-children.tsx`, which manages that class's **sections** and **subjects** side-by-side, each fetching on demand through the dependent 2.1 hooks (`useSections`/`useSubjects`, `enabled` only once expanded) with their own loading/empty/error/loaded states. Create/edit use one dialog per entity (`session-/class-/section-/subject-form-dialog.tsx`, RHF + Zod via shadcn `Form`); `422` maps to field errors via the shared `applyFieldErrors` helper, non-field messages show in a `FormBanner`, success toasts + closes, and submit can't double-fire. Destructive deletes confirm first through the reusable `delete-dialog.tsx` (error-token confirm button). Row actions live in a kebab menu (`row-actions.tsx`). **All writes** go through new mutation hooks (`hooks/academic/use-{session,class,section,subject}-mutations.ts`, re-exported from `hooks/academic/index.ts`) that **invalidate the shared 2.1 selector keys** (`["sessions"]`/`["classes"]`/`["sections"]`/`["subjects"]`) on success, so `SessionSelect`/`ClassSelect`/etc. and every downstream consumer stay consistent; a class delete also invalidates sections+subjects. Endpoints consumed exactly as the ticket maps them: `POST /sessions`, `PUT/DELETE /sessions/{id}`; `POST /classes`, `PUT/DELETE /classes/{id}`; `POST /classes/{class}/sections` + `PUT/DELETE /sections/{id}`; `POST /classes/{class}/subjects` + `PUT/DELETE /subjects/{id}`. New `lib/format/date.ts` (`formatDate`/`formatDateRange`) is the app-wide date formatter (exported from `@/lib/format`). All write actions are gated with `<Can permission="academic.manage">`. `npm run build` + TypeScript pass; `lint` clean (only the pre-existing accent/theme-switcher warnings). **Assumptions** (contract `docs/api/academic-structure.md` still absent): the write-permission slug is a single `academic.manage` across all four entities (`components/academic/management/permissions.ts` — adjust or split per entity when the contract lands); write payloads are `{name, start_date?, end_date?, is_current?}` (session), `{name, code?, numeric_value?}` (class), `{name, capacity?}` (section), `{name, code?}` (subject), all returning the standard envelope. **Branch scoping for super admin** is handled by the existing shell branch switcher — it folds the active branch into the shared read query keys (2.1) and the interceptor attaches `branch_id`, so switching branch re-scopes every list; a dedicated per-row branch column was not added because the read contract doesn't expose branch on each item (revisit if it does).
- [2.1] Academic shared selectors + read hooks: the cached dropdown sources every academic-context filter reuses. Read hooks in `hooks/academic/*` (`useSessions`, `useClasses`, `useSections(classId)`, `useSubjects(classId)`, re-exported from `hooks/academic/index.ts`) each wrap one read endpoint (`GET /sessions`, `GET /classes`, `GET /classes/{class}/sections`, `GET /classes/{class}/subjects`) through the shared `api` client, cache at `STALE_TIME.REFERENCE` (academic data is server-cached too), and fold the super-admin `branch` param into the query key so a branch switch re-scopes (the interceptor attaches `branch_id`; non-super-admin users send none). Module slugs are `sessions`/`classes`/`sections`/`subjects` so tasks 2.2/2.3 can invalidate `["sessions"]` etc. on write. The two dependent hooks key on `classId` (each class caches its own rows) and set `enabled: classId != null`, so before a class is chosen they don't fetch. Selector components in `components/academic/*` (`SessionSelect`, `ClassSelect`, `SectionSelect`, `SubjectSelect`, barrelled in `index.ts`) are thin maps of their hook's data onto a shared, fully-controlled `AcademicSelect` (value = numeric id or `null`). `AcademicSelect` owns the read-state presentations the ticket requires — **placeholder** (loaded/unselected), **loading** (spinner + "Loading…", disabled), **error** ("Couldn't load", disabled), **empty** (per-entity "No …", disabled), and **disabled** (e.g. `SectionSelect`/`SubjectSelect` show "Select a class first" and fetch nothing until `classId` is set). It uses React Query's `isLoading` (not `isPending`) so a disabled dependent query reads as disabled, not loading. Built on the base-ui `Select` primitive (`@workspace/ui/components/select`); the selected label renders in the trigger via the `SelectValue` function child. No screen mounts these yet — they're consumed by 2.2+ and the downstream modules; reuse is via these hooks/components so there is no duplicate fetch logic. `npm run build` + TypeScript pass; `lint` clean (only the pre-existing theme-switcher warnings). **Assumptions** (contract `docs/api/academic-structure.md` absent from the repo, like prior tasks): the four read endpoints return the standard envelope with `data` as an unpaginated array; item fields are `{ id, name }` (types in `types/academic.ts`), read defensively with an `… #id` label fallback. Read endpoints are available to all authenticated users (`feature-specs/06-academic-structure.md`), so no permission gate is applied to the selectors themselves. Adjust the field maps in the selector components if the real contract differs.
- [1.7] Dashboard: the placeholder `app/(app)/dashboard/page.tsx` is replaced by the real landing screen. The page (thin client component) renders a welcome header + `components/dashboard/dashboard-summary.tsx`, which fetches `GET /dashboard` via `hooks/dashboard/use-dashboard.ts` and renders one `SummaryCard` per figure the API returned — **rendering only present scalar figures and computing nothing client-side** (ticket Rules). `components/dashboard/figures.ts` (`toDashboardCards`) walks the response object, skips null/non-scalar/non-numeric entries, and maps each key through a `CATALOG` (label + Lucide icon + count/currency kind, with aliases covering the ticket's example figures: students/teachers counts, today's attendance, pending admissions, fee collection, profit/loss, income, expenses, assets, outstanding); unknown keys fall back to a humanised label and an inferred kind (money-word regex on the key, else a decimal-string value ⇒ currency). Money uses the new `lib/format` formatters: `formatMoney` groups the API decimal string with **pure string manipulation** (no float math, `৳` prefix, `code-standards.md` money rule) and `formatCount` groups integers via `Intl.NumberFormat`; values render in the mono/tabular font so the grid aligns. States: 8-card skeleton (`SummaryCardSkeleton`, mirrors the final card) on load, `EmptyState` when no figures, `ErrorPanel` (retry) on failure, and a distinct "No dashboard access" `EmptyState` on `403`. Super-admin branch/consolidated switches re-fetch because `useDashboard` folds `branchParam` into its query key (and the interceptor attaches `branch_id`); SHORT stale window since today's figures move during the day. `npm run build` + TypeScript pass; `lint` clean (only the pre-existing theme-switcher warnings). **Assumptions** (documented contract `docs/api/settings.md` absent from the repo): `GET /dashboard` returns the standard envelope with `data` as a flat object of scalar figures — counts as integers, money as `decimal(12,2)` strings. When the real contract lands, adjust the `CATALOG` keys/labels in `figures.ts`; the renderer, hook, and formatters need no changes.
- [1.6] Topbar global command search: the 1.5 `SearchPlaceholder` is replaced by `components/app-shell/global-search.tsx`, a `Command`-palette opened by `Cmd/Ctrl+K` (global keydown toggle), an `≥ md` input-style trigger, and a `< md` search-icon trigger — both open the same `CommandDialog`. Results split into **Modules** (the sidebar `NAV_GROUPS` flattened, permission-filtered via `useAuth().hasPermission` and substring-matched on label — shown in full when the query is empty so the palette doubles as quick-nav) and **records** from `hooks/search/use-global-search.ts`, which fans the debounced query (`hooks/use-debounced-value.ts`, 250 ms) across the permitted sources in `hooks/search/search-sources.ts` via TanStack `useQueries`. Record sources: Students (`GET /students?search=&per_page=5`, `students.view`), Teachers (`GET /teachers?…`, `teachers.view`), Classes (`GET /classes`, filtered client-side, `academic.view`). Built-in `Command` filtering is disabled (`shouldFilter={false}`); modules are matched in-component and records are server-filtered. Permission-filtering happens before any source is queried; a source error contributes no rows (retry off) rather than breaking the panel, and the API stays authoritative on select (`router.push(href)`). Loading ("Searching…" spinner) and empty ("No results for …" / "Type to search…") states live inside the panel; query keys fold in the super-admin `branch` param and `branch_id` is auto-forwarded by the API interceptor. Min query length 2 before records query. `npm run build` + `tsc` pass; `lint` clean (only the pre-existing theme-switcher warnings). **Assumptions** (backend `docs/api/*.md` absent — see auth note): record endpoints follow the standard Laravel list contract (`?search=`, `per_page`, envelope `data` array) and the field names read in `search-sources.ts` (`full_name`/`name`, `roll_number`/`student_id`, `designation`/`employee_id`, class/section names); record detail routes (`/students/{id}`, `/teachers/{id}`, `/academic?class=`) are owned by Phase-2 tasks. Adjust the source mappers/endpoints when the real contract lands.
- [1.5] App shell: authenticated route group now wraps pages in `AuthProvider` → `BranchProvider` → `AppShell`. `AppShell` owns a fixed topbar, fixed/collapsible desktop sidebar, `<lg` Sheet drawer, persisted sidebar collapse state, and a scrollable content area offset by topbar/sidebar dimensions. Sidebar navigation is grouped for Dashboard, Admissions, Students, Teachers, Academic, Attendance, Exams, Results, Promotion, Fees, Finance, Documents, Reports, and Settings; every item is permission-filtered via `useAuth().hasPermission` and active-route highlighted. Topbar includes the brand, 1.6 search placeholder, theme switcher, super-admin branch switcher, notifications placeholder, and user menu. Branch context lives in `components/branch/branch-provider.tsx` + `lib/api/branch.ts`; only super-admin sessions set `branch_id`, non-super-admin users only see a static branch label, and branch changes invalidate non-auth queries. `GET /branches` is implemented in `hooks/branches/use-branches.ts`. User menu reuses `ChangePasswordDialog` and `LogoutButton`, and routes Profile/Settings links. Verified with mocked auth/branches at 360px, 768px, and 1280px screenshots; `npm run build` passes (network access required for Next font fetch), `typecheck` passes, and `lint` passes with the existing theme/accent warnings only.
- [1.1] Design system: Tailwind v4 tokens in `globals.css` (`@theme inline`, light `:root` + `.dark`), two-axis theming (color mode via `next-themes`, accent via `data-accent` on `<html>` with pre-paint script + `AccentProvider`), Geist sans/mono fonts with tabular figures, `cn()` util, and a mount-guarded `ThemeSwitcher` dropdown. `npm run build` passes.
- [1.2] shadcn primitives + shared state/feedback components: shadcn primitives in `packages/ui/src/components/*` (Button, Input, Select, Dialog, Sheet, Table, Badge, Card, Tabs, DropdownMenu, Form/Field/Label, Sonner, Skeleton, Pagination, Calendar, Popover, Avatar, Command, Separator, Textarea, InputGroup) — CLI output, never hand-edited. App-level state/feedback in `apps/web/components/*`: `StatusBadge` (domain→state-token tones), `Button loading` wrapper, `ErrorBoundary` + `ErrorPanel`, `EmptyState`, `RouteProgress`, and `Table/Card/CardGrid/Detail` skeletons. Toast helpers in `lib/toast.ts` (`toastSuccess`/`toastError`/`getErrorMessage`, prefer API `message` + fallback, stable `id` to avoid stacking). Reusable `app/loading.tsx` + `app/error.tsx` + `app/global-error.tsx`. Layout wires `RouteProgress`, `ErrorBoundary`, and Sonner `Toaster`. `npm run build` + `lint` pass.
- [1.4] Auth: end-to-end Sanctum token auth. Route groups split `app/(auth)` (standalone login layout) from `app/(app)` (server guard in `(app)/layout.tsx` reads the httpOnly cookie via `lib/auth/session.ts` and `redirect("/login")` when absent). `lib/auth/session.ts` owns the `session_token` httpOnly cookie (set/get/clear via Next 16 async `cookies()`); `lib/auth/server-api.ts` is a tiny `fetch` envelope wrapper used only by server actions; `lib/auth/actions.ts` (`loginAction`/`logoutAction`/`clearSessionAction`) are the only writers of the cookie. `components/auth/auth-provider.tsx` pushes the token into the `lib/api` session bridge, registers the `401` handler (clear cookie + `/login`), fetches `GET /auth/me` once (TanStack Query, REFERENCE stale), and provides `useAuth()` (user + flat permission list + `hasPermission`); it gates the subtree behind loading/error so no authed UI renders early. `hooks/auth/use-permission.ts` (`usePermission`) and `components/auth/can.tsx` (`<Can permission>`) gate on permissions only. Login = RHF+Zod (`components/auth/login-form.tsx`) → `loginAction` → cookie set → `router.replace("/dashboard")` + `refresh()`; `422`→field errors, invalid creds→banner. Change-password = `hooks/auth/use-change-password.ts` + controlled `components/auth/change-password-dialog.tsx` (RHF+Zod, match rule, `422`→fields, success toast) for the 1.5 user menu. `components/auth/logout-button.tsx` calls `logoutAction` (revoke + clear + redirect). Placeholder `app/(app)/dashboard/page.tsx` proves the round-trip (greeting, permission list, `<Can>`, change-password, logout) until 1.5/1.7. Added `react-hook-form`/`zod`/`@hookform/resolvers` to `apps/web`; `globalEnv: ["NODE_ENV"]` in `turbo.json`. `npm run build` + `lint` (0 errors) + `typecheck` clean.
- [1.3] API client + server state: shared Axios instance in `lib/api/client.ts` (base URL from `NEXT_PUBLIC_API_BASE_URL`, `Accept: application/json`, request interceptor attaches `Authorization: Bearer {token}`, response interceptor unwraps the `{ success, message, data, meta? }` envelope and normalizes errors). Typed errors in `lib/api/errors.ts` (`ApiError`/`ApiValidationError` field→messages/`ApiForbiddenError`/`ApiNotFoundError`/`ApiNetworkError` retryable, + `is*` guards). `lib/api/session.ts` is the in-memory token bridge for auth 1.4 (`setApiToken`/`getApiToken`/`clearApiToken`/`setUnauthorizedHandler`); `401` calls `handleUnauthorized` → clears token + redirects to `/login` (default until auth overrides). `lib/api/query-keys.ts` documents+builds the `[module, action, params]` convention (`params` includes `branch` for super admin). `lib/api/query-client.ts` factory with defaults (no refetch-on-focus, retry only network/5xx once, `STALE_TIME` REFERENCE/STANDARD/SHORT). `components/query-provider.tsx` mounts `QueryClientProvider` (one client per session) wired into root layout. Envelope/`meta` types in `types/api.ts`. Reference hook `hooks/example-query.ts` proves the unwrap+meta round-trip. Axios + @tanstack/react-query added to `apps/web`. `npm run build` + `tsc` + `eslint` clean.

## In Progress

- None.

## Open Questions

- **Result-sheet PDFs (task 4.4):** backend ticket 8.4 (Marksheet PDFs via dompdf) is unfinished — no per-exam/annual result PDF route exists yet. Task 4.4 is blocked until the backend exposes the documented streamed endpoint; keep the download action gated/hidden until then.
- **Access control (task 6.6):** role→user and permission→role assignment was deferred by backend task 1.2 (managed via seeders). New backend ticket 15.1 now specifies the API (`docs/api/access-control.md`); task 6.6 is blocked until 15.1 ships — keep the screen hidden behind `role.manage` until then.
- **Parents create payload public ids:** frontend student resources expose only `public_id` strings, so task 2.8 sends those in `student_ids`. The inspected backend `ResolvePublicIds` middleware resolves scalar `student_id` fields but not `student_ids[]`; confirm the deployed backend resolves `student_ids.*` or patch that middleware/request before relying on parent creation with selected public ids.
- Add unresolved product or implementation questions here.

## Architecture Decisions

- Next.js 16 (App Router) + React 19 + Tailwind v4 + shadcn/ui. CSS variables in `globals.css` via `@theme inline`; light + dark via `.dark` selector. No `tailwind.config.js`.
- Sanctum bearer-token auth; token stored in an httpOnly cookie set server-side.
- TanStack Query for server state; shared Axios client in `lib/api` for all calls.
- Permission-based UI gating (never role-name checks). API `401/403/404` is the authoritative access boundary.
- `components/ui/*` are generated by the shadcn CLI — do not modify them manually.

## Session Notes

- **Results: admin/student marksheets now use the same logo-only watermark format as public (2026-07-08):** `ResultMarkSheet` already renders the centered school-seal watermark shared by all result surfaces, but the admin/student adapter (`ExamMarkSheet`) was also passing `passed` / `published`, which could add the extra diagonal `FAILED` / `PROVISIONAL` text overlay that the public sheet never showed. The adapter now omits those props, so public, admin, and student marksheets all share the same watermark format while publication/pass state continues to show in the surrounding UI badges instead of the sheet overlay. `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean aside from the existing 18 warnings.
- **Results: grading legend now stretches to the particulars block like the admin sheet (2026-07-08):** the shared details-and-legend row on `ResultMarkSheet` previously used `alignItems: flex-start`, so the public sheet’s extra Session / Semester / Date of Birth rows made the left particulars column taller while the grading legend stayed short. Switched the legend into a fixed-width stretched column and let the table fill that height, so the public legend’s vertical span and placement now match the admin mark sheet more closely. Mirrored the same layout in `result-mark-sheet-document.ts` so the generated PDF stays aligned with the screen version. `npm run typecheck --workspace web` clean; `npm run lint --workspace web` clean aside from the existing 18 warnings.
- **Results: public marksheet grading legend fixed to the approved static table (2026-07-08):** `PublicResultView` now passes a local `PUBLIC_MARKSHEET_SCALE` into `ResultMarkSheet`, so the public transcript and its print/download PDF always show the supplied grading legend rather than whatever `grading_scale` the API returns. The fixed legend rows are `80–100 A+ 5.00`, `70–79 A 4.00`, `60–69 A- 3.50`, `50–59 B 3.00`, `40–49 C 2.00`, `33–39 D 1.00`, `0–32 F 0.00`. Admin/student sheets are unchanged and remain on their existing grading-scale path. `npm run typecheck --workspace web` clean.
- **Results: plum border default + public sheet gains grade points & grading legend (full-stack, 2026-07-08):** switched the mark-sheet default `borderTheme` from `sage` to **`plum`** (screen + PDF) to match the imported design's guilloché colour across all three surfaces. Closed the last public-vs-admin gap — the public transcript now shows the **Grade Point column** and the **Range of Marks / Grade / Point legend**. Because the public page is unauthenticated and `GET /grading-scales` is `auth:sanctum`-only, the scale is **bundled into the public result payload**: backend (`~/Herd/app-api`) `ResultService::publicResult()` now also returns `grading_scale` (`GradingScale::orderByDesc('min_marks')`), and `PublicResultResource` exposes per-subject `grade_point` (already snapshotted on `Mark`) plus `grading_scale` via `GradingScaleResource::collection`. Frontend: `PublicResultSubject` gained `grade_point`, `PublicResult` gained optional `grading_scale: GradingBand[]`, and `PublicResultView` maps the point into the shared marksheet; follow-up UI work now locks the public legend to the approved static `PUBLIC_MARKSHEET_SCALE` so the rendered/printed public sheet matches the supplied school table exactly. `PublicResultReadsTest` extended (seeds two bands; asserts `grade_point` + ordered `grading_scale`) — 6/6 green, Pint clean; frontend `tsc` + `eslint` clean.
- **Results: exam-title styling + public sheet unified with admin/student (2026-07-08):** the exam-title block now matches the design exactly — the title renders **uppercase**, 15px/`.04em`, with the italic "(Held in the Month of …)" (or session subtitle) directly beneath it (dropped the extra non-italic subtitle line) — in both the on-screen sheet and the PDF. The **public result sheet** was diverging from the admin/student sheet (it fed a settings-derived school name/logo, a "Session …" subtitle, a "Result / GPA" label, and different field labels); it now renders identically — default institution + star seal, no subtitle, plain **GPA** label, and the same field order/labels ("Father's Name"/"Mother's Name", Student/Class/Section/Roll first, then public-only Session/Semester/Date of Birth). At that point the remaining public-vs-admin differences were data-only; later follow-ups added public grade points and the grading legend as well. `tsc` + `eslint` clean.
- **Results: rebuilt `ResultMarkSheet` to the imported BTEB "Result Mark Sheet" design across all three surfaces (2026-07-08):** replaced the design-system-styled transcript with a faithful reproduction of the Claude Design `d78bfe47…` "Result Mark Sheet" (Bangladesh Technical Education Board academic transcript). The sheet is now a **fixed-palette paper document** that deliberately ignores the app light/dark theme so screen and print match: a **guilloché diagonal-stripe border** (4 presets — sage/slate/sand/plum, default sage), parchment paper (`#faf7ee`) with an inset hairline, an **embossed red star seal** (radial-gradient, or the school logo when present), a serif **Playfair Display** institution header + dark-red "ACADEMIC TRANSCRIPT" label, an optional purple **SL No** line, centered exam title + "(Held in the Month of …)", left student particulars (label col 150px, `:  value` bold, EB Garamond) beside the server-owned **Range of Marks / Grade / Point** legend, a heavy-ruled **subjects table** (adaptive Subjects / Marks / Credit Hours / Grade Letter / Grade Point columns), a **boxed GPA** (mono, grade appended), a footer with **Date of Publication / Date of Issue** and two **Caveat** script signature lines ("Compared by" / "Controller of Examinations"), a diagonal **status watermark capability** (Duplicate/Provisional/Failed when explicitly requested through the sheet props), and the italic red "issued without any alteration or erasure" footnote. Three Google faces (**Playfair Display, EB Garamond, Caveat**) added to `app/layout.tsx` via `next/font` as `--font-serif-display` / `--font-serif` / `--font-script` CSS variables (font-display swap → only fetched on pages rendering the sheet). Props kept backward-compatible (same `fields`/`scale`/`subjects`/`gpa`/`gpaLabel`/`grade`/`passed`/`published` contract) so **public lookup**, **admin result search**, and **student-detail semester result** all inherit the look unchanged, plus new optional design props (`slNo`, `examMonth`, `publishDate`, `issueDate`, `documentStatus`, `borderTheme`, `signature1/2`, `schoolAddress`). **Header institution:** the name renders as a two-line block (name + address, Playfair) and — until the settings feature supplies the real school name/logo — defaults to the imported design's reference school **"Hazi Jabed Ali Memorial School" / "Saratgonj, Chatmohar, Pabna"** with the red star seal (`resolveInstitution` helper). The admin/student-detail surfaces **no longer feed the branch name** as the header (branch ≠ school-level setting), so they show the default until settings lands; the public sheet still reads `usePublicSettings()` school name/logo when present. Data reality: BTEB-only fields absent from our API (Technology code, Registration No, per-subject Credit Hours, signatures, publish/issue dates) render only when supplied — footer dates show `—` when unknown. **Print/Download** rewritten to match (`result-mark-sheet-document.ts` builds the same offscreen transcript reusing the `:root` next/font variables, rasterizes via html2canvas → single-page A4 jsPDF); the guilloché/seal gradients degrade to solid fills in html2canvas 1.4.1 but colours hold. `tsc --noEmit` + `eslint` clean.
- **Results: academic-transcript mark-sheet layout + Print/Download PDF across all three result surfaces (2026-07-08):** introduced a shared, print-friendly `ResultMarkSheet` presentational component (`components/results/result-mark-sheet.tsx`) styled after a BTEB academic transcript but grounded in the design system (surface card + double-ruled `accent-soft-border` inner frame, centered school/board header with logo fallback, two-column student particulars — **including Father's/Mother's Name** — beside a server-owned **Range of Marks / Grade / Point** grading-scale legend, a bordered subject table with adaptive Code/Marks/Grade/Point columns, a footer GPA/verdict/publication badge row, and decorative "Prepared By / Controller of Examinations" signature lines). It takes normalized props (`fields`, `scale`, `subjects`, `gpa`, `passed`, `published`) so one layout serves every surface; grade letters/points are tone-colored (A→success, B→info, C/D→warning, F→error). Wired into **(1) admin individual result** and **(2) student-detail all-semester result** — `ResultBundlePanel` now renders each `PerExamResult` as a mark sheet via a new `ExamMarkSheet` adapter (replacing the old `ResultHeader` + `ExamResultCard`), pulling the grading scale from `useGradingScale()` (absent/forbidden for the viewer → the legend just hides) and the school name from `useAuth().user.branch?.name`; the cross-semester `AnnualResultCard` summary is unchanged. Wired into **(3) public result lookup** — `PublicResultView` maps `PublicResult` into the same sheet (school name/logo from `usePublicSettings()`, semester label, initially without the grading legend/verdict until later follow-ups added public grade points and the legend), replacing the bespoke `StudentInformation` + `SubjectResults` tables. **Parents:** `ResultBundleResource` (`~/Herd/app-api`) now returns `father_name`/`mother_name` (from `student.father_name_en`/`mother_name_en`) and `ResultStudentSummary` gained the optional fields; the public payload already carried them (`ResultReads`/`ExamResults`/`AnnualResults`/`ResultBundle` suites 43/43 green, Pint clean). **Actions (Print / Download / Share):** each sheet carries **Print**, **Download**, and **Share** buttons (top-right row, `print:hidden`, disabled/spinner while busy). Print + Download are functional now via a new `result-mark-sheet-document.ts` that mirrors the admission-PDF pattern — it builds a self-contained transcript HTML string with plain **hex** colors (no Tailwind/oklch, which html2canvas 1.4.1 can't parse), rasterizes it offscreen with `html2canvas` (already a dep), and embeds it into a single-page A4 `jsPDF`; **Download** saves `<name> - <exam>.pdf`, **Print** calls `doc.autoPrint()` + opens the blob (falling back to a save if the popup is blocked), each with success/error toasts. **Share** remains a placeholder (toasts "Result sharing will be available soon.") pending the real share flow. `tsc` + `eslint` clean.
- **Results (4.3): exam-results browse re-keyed to Branch → Exam → Semester → Class, search-gated, with a pass/fail summary strip; generate/publish buttons removed:** the "Exam results" tab on `/results` (`components/results/results-page.tsx` `ExamResultsPanel`) now mirrors the coordinates-search cascade — **Branch** (super-admin only, screen-local "All branches"), **Exam** (distinct names from `useExams`), **Semester** (the name's `type` variants, auto-selected when only one, disabled until an exam is picked), **Class (required**, via the shared `SearchClassPicker` scoped to the exam's targeted classes; Search stays disabled without it), and the existing **Verdict** filter (All / Passed / Failed, default All, live after search) — replacing the old combined exam picker + class-for-section + section selects. Results load only on **Search** (the exam/class/branch tuple is frozen at submit; verdict + pager stay live against it). A **summary strip** above the result sheet shows **Total examinee / Total passed (+%) / Total failed (+%) / Total GPA-5 (+%)** — percentages are each figure's share of the examinees (`formatShare`, one decimal), rendered right of the number in the card's tone (passed green, failed red, GPA-5 brand). **Backend** (`~/Herd/app-api`): `GET /exams/{exam}/results` now returns `meta.summary = {examinee, passed, failed, gpa5}` (`ResultService::summarizeExamResults`, one aggregate query over the same class/section scope as the listing but **ignoring `is_passed`** so the strip describes the whole cohort; `gpa5` counts `gpa >= 5`), and the endpoint gained a **`class_id` filter** (`ListExamResultsRequest` + `examResultsQuery`, narrowing via `enrollment.class_id` — the shared scope behind both the listing and summary). New feature test `test_browse_summary_and_class_filter` (summary ignores the verdict filter; own class matches all, another class zeroes out; `class_id` accepted as both an integer id and the public-id hash the client sends, proving the `ResolvePublicIds` path) — `ExamResultsTest` 12/12, `ResultReadsTest`+`AnnualResultsTest` 25/25, Pint clean. The **Generate / Publish buttons, confirm dialog, and permission note were removed** from this panel (annual actions keep theirs; the exam generate/publish hooks remain in `hooks/results` but are currently unused), so `ExamResultsPanel` no longer takes `canGenerate`. Plumbing: `ExamResultsParams` gained `class_id?` + `branch_id?` and `cleanExamResultParams` forwards them (branch wins over the active branch in the interceptor); `types/result.ts` gained `ExamResultsSummary` + `ExamResultsMeta` and `useExamResults` types its `meta` with the summary. **Student names in the result sheet link to the detail Results tab** (mirroring the mark-entry grid): `ExamResultResource` gained `student_id` (the student's `public_id`; student already eager-loaded), `ExamResultRow.student_id?` read defensively, and `ResultStudentLink` renders the name as a `Link` to `/students/{id}?tab=results` in both the desktop table and mobile cards (plain text when the id is absent). `tsc` + `eslint` clean; `npm run build` green.
- **Results (4.3): class-coordinates search re-keyed to Branch → Exam → Semester → Class → Section → Roll:** the staff "Class coordinates" mode on `/results` (`components/results/results-page.tsx` `StaffSearchPanel`) no longer asks for a session directly — it mirrors the mark-entry cascade: **Branch** (super-admin only, screen-local, "All branches" default; changing it resets the whole selection), **Exam** (distinct names from `useExams`), **Semester** (the `type` variants under that name, auto-selected when only one exists, disabled until an exam is picked), **Class** (new `SearchClassPicker`, scoped to the exam's targeted classes or all classes via `ClassSelect branchId`), **Section (optional** — "All sections" `clearLabel`; a class without sections searches by roll alone), and **Roll**. The resolved exam pins the search's `session_id` (exam name + semester → exam record → its `session_id`), so the submit payload to `GET /results/search` is unchanged in shape: `{session_id, class_id, section_id?, roll_no, branch_id?}`. Section is no longer required by `canSubmit` (was mandatory). **Backend** (`~/Herd/app-api`): `GET /results/search` previously 422'd without `section_id` — `SearchResultsRequest` now requires only (session, class, roll) with `section_id` optional, and `ResultService::searchEnrollment` filters by section only when given; because rolls are unique per (session, class, section) bucket, an omitted section that matches the same roll in two sections is a **422 on `section_id`** ("select a section") rather than a silent first match, and a sectionless class resolves by roll alone. New feature tests: coordinates-without-section bundle, ambiguous-roll 422 + section-narrowed retry (`ResultReadsTest` 17/17 green, `DemoSeederTest` green, Pint clean). Plumbing: `ResultSearchParams` gained `branch_id?`, `cleanSearchParams` forwards it (interceptor lets it win over the active branch) and the query key folds `branch_id ?? branchParam`; `useExams` gained an `enabled` flag (default `true`) so the exam list only fetches in coordinates mode. **Semester-scoped display:** a coordinates search shows *only the picked semester's* exam card — `ResultBundlePanel` gained an optional `examType` prop (filters `bundle.exams` client-side, hides the cross-semester annual card, header "Exams" count reflects the filtered set, semester-specific empty copy); the panel captures the semester at submit (`submittedSemester`, frozen so tweaking pickers doesn't re-filter shown results) and an admission-no search still shows the full bundle + annual. `tsc` + `eslint` clean.
- **Mark entry (4.2): exam + semester pickers, branch-switch staleness fix:** the mark-entry grid (`components/marks/mark-entry-grid.tsx`) now selects **Exam → Semester → Class → Section**. The Exam dropdown lists **distinct exam names** from `useExams`; the new Semester dropdown (disabled until an exam is picked) offers the `type` variants (`EXAM_TYPE_LABELS` — First semester / Second semester / Final) that exist under that name, auto-selected when there's only one, and the name+type pair resolves to the actual exam record (`examId` is now derived, not state). Changing either resets class/section/draft. Note: names repeating across sessions resolve to the first match — revisit if the exam list ever spans multiple sessions. **Branch fix**: switching the super-admin branch re-scoped the exam list (`useExams` folds `branch` into its key and the interceptor sends `branch_id`) but the selected exam/class/section survived, so the grid kept showing another branch's matrix — same staleness bug fixed for attendance earlier. An effect now clears the whole selection (and draft/errors/banner/search/page) when the selected exam name is no longer in the re-scoped list, mirroring the attendance-sheet pattern. The Section picker now defaults to a selectable **"All sections"** (`clearLabel`, like the attendance sheets) instead of a "(optional)" label. A **Branch** picker (super-admin only, default "All branches") leads the selector row as a **screen-local** filter — deliberately *not* wired to `setActiveBranch`, so it never re-scopes the navbar switcher or other screens (`null` inherits the navbar's scope; changing it resets the whole selection + draft). Plumbing: the request interceptor now lets an **explicitly passed `branch_id` param win** over the global active branch (`{branch_id, ...config.params}`), `ExamListParams`/`MarkMatrixParams` gained `branch_id?`, `useExams`/`useMarkMatrix` send it and fold `branch_id ?? branchParam` into their keys, `useClasses(branchId?)` + `ClassSelect branchId` prop forward it for the all-classes exam path, and `BranchSelect` gained the `clearLabel` passthrough. **Student names link to the detail Results tab**: matrix rows now carry `student_id` (backend `MarkMatrixResource` adds the student's `public_id` — `~/Herd/app-api`; `MatrixStudent.student_id?` read defensively), and both grid views render the name via `StudentNameLink` → `/students/{id}?tab=results` (plain text when the id is absent). `tsc` + `eslint` clean; backend mark suites pass (only the pre-existing payment/subject 404 failures, confirmed on a clean tree).
- **Attendance entry: optional section (default All sections) + branch-aware (both ends):** the Entry tab now filters like the Class sheet. **Backend** (`~/Herd/app-api`): `GET /attendance/sheet` takes a required `class_id` + optional `section_id` (omitted → whole-class roster ordered section → roll, each row carrying its `section` name; top-level `section` is null) and validates the class against the super-admin `branch_id` (via `FiltersByBranch`) so an out-of-branch class 422s instead of leaking another branch's roster; `POST /attendance` accepts `section_id` **or** `class_id` (`required_without` each other) — a class-scoped save validates enrollments as active members of the class, and `AttendanceService::saveBulk(?$sectionId, ?$classId, …)` resolves the assignment-check class from either. New feature tests: whole-class sheet, sheet branch rejection, whole-class save, missing-both 422, cross-class enrollment rejection. **Frontend**: entry screen enables on class + date (`useAttendanceSheet` no longer requires section), SectionSelect defaults to "All sections" (`clearLabel`), save/autosave send `class_id` + `section_id` only when set, whole-class rosters show "Sec X" beside each roll (desktop cell + mobile chip) and the header reads "Class · All sections". **Branch-switch staleness fix (both attendance tabs):** when the super admin switches branch, the class list re-scopes — a selected class no longer in it is now auto-cleared (entry also restores from localStorage, which previously pinned another branch's class and kept showing its roster). `tsc` clean; backend attendance/TC suites show only the pre-existing 404 failures confirmed on a clean tree.
- **Attendance browse: branch filter + optional section (both ends):** two fixes to the class monthly attendance sheet. (1) **Branch filtering** — `GET /attendance` ignored the super-admin `branch_id` convention (BranchScope is bypassed for super admins, and the explicit filter was never applied), so the attendance surface showed all branches regardless of the selected branch. **Backend** (`~/Herd/app-api`): `ListAttendanceRequest` now uses `FiltersByBranch`, `AttendanceController::index` passes `withBranchFilter(...)`, and `AttendanceService::list` narrows through `enrollment.student.branch_id` when set; covered by a new feature test (`test_super_admin_browse_narrows_by_branch_filter`: unfiltered = both branches, narrowed = one, non-super-admin `branch_id` ignored). (2) **Section optional** — the Class sheet previously required class **and** section. Now a class alone loads the whole class ("All sections"); picking a section narrows it. `useClassAttendanceMonth` enables on class only and omits `section_id` when unset; `AcademicSelect` gained a `clearLabel` prop (selectable empty option) that `SectionSelect` passes through as "All sections"; rows carry their section (new `section` field on `AttendanceResource` — `enrollment.section` eager-loaded in `list()`/`update()` for strict mode — and on `AttendanceRecord`/`ClassAttendanceRow`), shown as "Roll N · Section X" and as a CSV column in the all-sections view, with rows sorted section → roll → name. The **Entry** tab still requires a section (saving is per-section by design). `tsc` + `eslint` clean; attendance suites pass apart from 8 pre-existing failures confirmed present on a clean tree.
- **Roll numbers unique per class + section (both ends):** rolls are now scoped to the **session + class + section** bucket, so each section of a class numbers independently from 1 (Class 6 A and B can both have rolls 1, 2, 3…), and a sectionless class keeps one class-wide sequence (null section = its own bucket). **Backend** (`~/Herd/app-api`): `AdmissionService::approve`'s auto-roll `max('roll_no')` query now filters by `section_id` (previously class-wide, so section B's first student got the class max + 1); `StoreStudentRequest` + `UpdateEnrollmentRequest` made `section_id` nullable (matching the earlier approve change + the nullable `enrollments.section_id` migration) with null-safe roll-uniqueness checks — the enrollment-update duplicate query also gained the missing `class_id` filter so the null-section bucket can't collide across classes (and `integer('section_id')` no longer coerces null → 0, which had skipped the duplicate check entirely for sectionless rows); `StudentService::create` tolerates the omitted section key. Feature test updated: `test_omitted_roll_auto_assigns_next_in_section` (section B starts at 1 and continues from its own max). **Frontend**: `StudentCreateInput.section_id` + `EnrollmentUpdateInput.section_id` → `string | null`; the student create dialog and the enrollment-history inline editor no longer require a section (labels/descriptions say "Optional — only if the class has sections"); the approve dialog's roll hint now reads "next roll in the selected section". Known gap left for a follow-up: promotion (`IndividualPromotionRequest`/`BulkPromotionRequest`) still requires a target section, and bulk "keep rolls" can collide when two sections merge into one target section. `tsc` + `eslint` clean; admission/student API suites pass (pre-existing unrelated 404 failures across the backend suite confirmed present on a clean tree).
- **Admission approval (2.6) — auto roll + parent defaults:** the approve dialog (`components/admissions/review/approve-dialog.tsx`) now defaults **Create a parent account → checked** with **relation → Father** (re-checking the box re-seeds "father"; unchecking clears it), and the **roll number is optional**: left blank, the API assigns the class's next roll (last roll in the class for the selected session + 1, class-wide so per-section uniqueness still holds), mirroring the admission-no "Auto-generated" pattern. The **section is also optional** (a class may have no sections): the select stays enabled for classes that have them, blank enrolls under the class only. `AdmissionApproveInput.roll_no` → `number | null`, `section_id` → `string | null` (`types/admission.ts`). **Backend change** (`~/Herd/app-api`): `ApproveAdmissionRequest` `roll_no` + `section_id` `required→nullable` (roll uniqueness kept, null section = its own bucket; section-belongs-to-class checked only when given), `AdmissionService::approve` computes the next roll inside the transaction with a `lockForUpdate()` max-query so concurrent approvals can't collide and tolerates a null section, `ApprovedStudentResource` null-safe on section, plus migration `2026_07_02_100000_make_enrollments_section_nullable` (`enrollments.section_id` nullable); covered by new feature tests (empty class starts at 1; continues from the class max across sections; sectionless approval). The request already defaulted `create_parent_account=true` + `parent_relation=father` server-side when omitted — the UI now matches.
- **Admissions soft-delete + trash (2.6 follow-up):** the admissions review queue now supports **soft delete (single + bulk)** and a dedicated **Trash** surface with **restore** and **permanent delete** (single + bulk). New mutations in `hooks/admissions/use-admission-mutations.ts`: `useDeleteAdmission` (`DELETE /admissions/{id}`), `useBulkDeleteAdmission` (`POST /admissions/bulk-delete {ids}`), `useRestoreAdmission` (`POST /admissions/{id}/restore`), `useBulkRestoreAdmission` (`POST /admissions/bulk-restore {ids}`), `useForceDeleteAdmission` (`DELETE /admissions/{id}/force`), `useBulkForceDeleteAdmission` (`POST /admissions/bulk-force-delete {ids}`) — all invalidate `["admissions"]` (live queue + detail + trash share that key root). New `hooks/admissions/use-trashed-admissions.ts` reads `GET /admissions/trash` (paginated, search + class filters, no status param; reuses the exported `toAdmissionQuery` from `use-admissions.ts`). `types/admission.ts` `Admission` gained `deleted_at?`. UI: `admissions-list.tsx` got page-scoped selection checkboxes (shared new `components/select-checkbox.tsx`), a per-row trash button (any status), a bulk-delete action bar, and a **Trash** header link — all gated by `admissions.manage`; soft delete confirms via the shared `DeleteDialog` with reversible "move to trash" copy. New `components/admissions/review/admissions-trash.tsx` + route `app/(app)/admissions/trash/page.tsx` (static segment beside `[id]`, gated on `admissions.manage`): restore runs directly (reversible, toast); permanent delete (force) confirms first. **Endpoint convention assumed** (admissions contract still absent): standard Laravel soft-delete REST + the project's existing `bulk-delete` pattern (cf. exams) — confirm against the real backend when the contract lands. `tsc` + `eslint` clean on all touched files.
- **Student detail (2.7) hero facts + branch:** the hero key-facts strip now shows **Class · Section · Roll no · Admitted** (derived from the current/active enrollment) instead of Admission no · DOB · Religion · Admitted — DOB/Religion/Admission still render in the Profile card below. The hero **subtitle now shows the branch name** instead of `Admission {no}`. This required a **backend contract change** (`~/Herd/app-api`): `StudentResource` gained a `branch` key (`whenLoaded('branch')` → `{id: public_id, name}`), and `StudentService::loadProfile` + `::update` now eager-load `'branch'`. Frontend `types/student.ts` `Student` gained `branch?: NamedRef | null`.
- **Student detail (2.7) tabbed UI:** the `/students/[id]` page now puts a professional **tab strip below the hero** instead of routing section links out of the hero menu. New shared kit `components/detail/detail-tabs.tsx` — `DetailTabs` (a bordered `bg-surface` segmented bar, active tab = filled `bg-brand` pill; tabs spread `flex-1` from `sm` up) + `useDetailTab(tabs, param="tab")`, which reads/writes the active key from a **URL search param** (`?tab=`) so a **refresh keeps the active tab**; the first tab (Profile) is the default and writes a clean URL (param deleted). **The bar never scrolls** — a `ResizeObserver` measures natural tab widths against the available width (hidden measurer row) and overflows tabs that don't fit into a trailing **More** dropdown (the "priority+" pattern), so mobile shows as many tabs as fit + a menu for the rest (the More trigger reflects the active tab when it's overflowed). Tabs are permission-gated: **Profile** (always, default) · **Attendance** (`attendance.view`) · **Results** (`result.view`) · **ID card** (`idcard.generate`) · **TC** (`tc.view`). Profile renders the existing identity/guardian/address cards + enrollment history; **Attendance** renders inline via a new embeddable `components/attendance/student-attendance-panel.tsx` (the `MonthlySheetFrame` + `useStudentMonthlyAttendance` without the duplicate hero); Results/ID card/TC render a `ComingSoonPanel` (`EmptyState`) since those modules (tasks 4.x/6.x) aren't built — the section links previously pointed at dead routes. The tab body sits behind a `React.Suspense` boundary (for `useSearchParams`). **Change photo / Deactivate / Edit stayed as hero actions** (per request) — the section nav was removed from the hero `DetailActions` menu, leaving only the manage actions there. `tsc` + `eslint` clean.
- Backend API contracts are fixed in `docs/api/*.md`; implement against them, do not invent endpoints.
- **Auth contract (1.4):** the ticket references `docs/api/auth.md`, which is absent from the repo. Implemented against the standard envelope + Sanctum shape documented across the context files. Assumptions to confirm against the real backend: `POST /auth/login` accepts `{ email, password }` and returns `{ token, user }` (also tolerates `access_token`); `GET /auth/me` returns the user with flat `roles: string[]` + `permissions: string[]`; `POST /auth/change-password` takes `{ current_password, password, password_confirmation }`; `POST /auth/logout` revokes the current token. If the backend uses ID/username login instead of email, adjust `login-form.tsx`’s field + `loginAction` body and the `422` field mapping. `<Can>` demo on the placeholder dashboard gates on `dashboard.view`.
- **Teacher assignments (2.3) fixes:** corrected two assumptions from the original 2.3 note. (1) The assignment create/edit dialog now includes a **required `session_id`** (`SessionSelect`, defaulting to the `is_current` session on create) — the payload type `TeacherAssignmentInput` requires it. (2) The assignments **list** now reads labels via `assignmentLabels()` (`types/academic.ts`), which resolves class/section/subject/teacher from either the flat `*_name` fields **or** nested relation objects (`class`/`school_class`/`section`/`subject`/`teacher`), since the API expands relations as nested objects rather than flat labels (class/section/subject were rendering empty). Confirm relation key names against the real contract when it lands.
- **Photo upload UX (profile + teachers):** extracted a shared `components/image-dropzone.tsx` — a keyboard-accessible picker supporting both **click-to-select and drag-and-drop**, with configurable `accept`/`maxBytes` and the same jpg/png + ≤ 2MB client validation. Both photo dialogs (`profile-photo-dialog.tsx`, `teacher-photo-dialog.tsx`) now render the dropzone **only when no image exists**; when a photo is present (existing or a newly picked file) they show the image with a **"Remove image"** action. Per the requested behaviour, "Remove" only clears the dialog — the saved photo is **deleted on save**, not before: removing the existing photo and clicking save issues `DELETE /auth/photo` / `DELETE /teachers/{id}/photo` via new `useDeleteAvatar` / `useDeleteTeacherPhoto` hooks (invalidate `["auth","me"]` / `["teachers"]`). The save button switches between "Save"/"Uploading…" and "Remove photo"/"Removing…", and is disabled when there's no change. **Assumption** (contracts still absent): photo delete is the RESTful counterpart of the existing upload route. Adjust when the contract lands. TypeScript + `eslint` clean on the touched files.
- **Public admission (2.5) refinements:** (a) `/public/settings` returns classes **nested under each branch** (`branches[].classes[]`, no `branch_id` on classes) — types + the class selector + preview read from the selected branch's nested `classes`. (b) The photo preview object URL is created/revoked inside one `useEffect` (StrictMode-safe). (c) **Download Application** generates the PDF **client-side** (the server PDF route isn't available): the submitted snapshot is rendered as a formal printed-form layout, rasterized with `html2canvas` (preserves the design **and** Bangla text, which jsPDF's built-in fonts can't draw), and saved via `jspdf` as a direct multi-page download — added deps `jspdf` + `html2canvas` to `apps/web`. Swap back to a server-PDF link when the backend route lands. **The Preview step and the PDF share one source of truth** — `application-template.ts` `buildApplicationHtml()` builds the entire document markup+CSS once (navy header with logo/app-no/class badge, info card + affix-photo box, bordered Student/Guardian/Address field grids, Previous Education table, declaration + signature lines, fixed `DOC_WIDTH=800`). The PDF rasterizes it offscreen; the Preview injects the same HTML and CSS-`transform`-scales it to fit the wizard column, so the two **cannot drift**. (d) **Address step redesigned**: Present + Permanent each a cascading **Division → District → Upazila → Post office** set (bundled offline dataset at `lib/geo/bd-locations.json` — 8 divisions / 64 districts / 485 upazilas / 1346 post offices, sourced from aiFdn/Postcodes-of-Bangladesh, Mymensingh division patched in) via `lib/geo` accessors + a string-valued `location-select.tsx`, plus a free-text village/street and a **"Same as present"** checkbox that mirrors (and keeps synced) the permanent address. **Field-name change** (per request): the address now submits `present_division/district/upazila/post_office/village` + `permanent_*` (single set each, **no bn/en split**) — replaces the old `present_*` + `permanent_*_bn/_en`. Confirm these field names against the real backend contract when it lands. (e) **Wizard chrome + structure aligned to the "Admission form redesign" Claude Design handoff**, recreated with shadcn + theme tokens (no hardcoded hex, per `ui-context.md`): a **progress bar** above the stepper (`bg-subtle` track + `bg-brand` fill, `{pct}% complete` label, width `round(step/6·100)%`, `role="progressbar"`); the step heading uses an uppercase **"Step N of 7"** eyebrow under the title, separated by a hairline; the current step circle gains an accent focus ring. Sub-group headings (Guardian Father/Mother, Address Present/Permanent) now use a shared `SectionLegend` (`fields.tsx`) matching the design's uppercase accent (`text-brand`) divider. The Documents step is a **two-column** layout (photo left, supporting documents right; `grid lg:grid-cols-2`) per the design. The rest already matched the spec (centered card, clickable stepper, 2-col field grids, navy preview banner via the shared print template, success confirmation) and was left intact. **Exact design palette + font**: the public admission page (`app/admissions/application/page.tsx`) now carries the handoff's literal look — fixed light navy-on-white institutional palette (`#eef1f6` page, `#13294b` ink, `#2f6fed` accent, `#e5484d` error) in **Libre Franklin** (`next/font/google`). Implemented without hardcoding hex in components: an `admissionTheme` style object re-points the design-system token CSS vars (`--bg-base`, `--text-primary`, `--accent-primary`, etc.) on the page wrapper — the same mechanism as the app's `[data-accent]` themes — so every shadcn primitive and `bg-*`/`text-*` utility inside inherits the palette. Scoped to this route only (rest of app untouched) and pinned to the light palette regardless of dark mode.
- **List tables + app-shell aligned to the "School MS" Claude Design handoff:** applied the imported design to the admissions, students, and teachers list tables and the shell — recreated with theme tokens (no hardcoded hex, per `ui-context.md`) so light/dark + accent themes still work. (1) Shared `Table` primitive (`packages/ui/src/components/table.tsx`): header row now has a `bg-muted/60` fill with **uppercase, tracked, `text-xs` muted** column labels and `h-11 px-4` cells (was `h-10 px-2`, non-uppercase); body cells `px-4 py-3.5`. The three list table cards gained `overflow-hidden … shadow-sm` so the header fill clips to the rounded corners. (2) `StatusBadge` (`components/status-badge.tsx`) is now a **pill with a leading state-colored dot** (`gap-1.5 px-2.5 font-semibold`) — applies everywhere the badge is used. (3) New shared `components/list-pager.tsx` (`ListPager`) replaces the three near-identical local `Pager` copies: left **"Showing X–Y of Z <unit>"** summary (driven by `PaginationMeta.from/to/total`) + right windowed **numbered page buttons** (active = filled primary) flanked by Prev/Next chevrons. Wired into all three lists with `unit="application" | "student" | "teacher"`. (4) Sidebar nav active item (`app-shell/sidebar-nav.tsx`) is now a `rounded-lg` `bg-accent-dim` **semibold** pill. The shell's topbar/sidebar/grouped-nav structure already matched the handoff and was left intact. **Note:** kept the app's existing blue accent token rather than the handoff's literal indigo — the design's structure is reproduced through the token system, not a global re-color. `tsc` + `eslint` clean on all touched files.
- **App-shell relayout + table/topbar polish (School MS handoff, follow-up):** (1) **Shell layout** now matches the handoff exactly — a **full-height sidebar** (`app-shell/sidebar.tsx`) with the **brand header at its top** (aligned to the `h-16` topbar, `border-b`), and a **content column** (`flex min-w-0 flex-1 flex-col`) whose `Topbar` sits above a scrollable `main`. Replaced the previous full-width fixed topbar + `fixed top-16` sidebar + `pt-16 lg:pl-64` offset model with a flex `h-svh overflow-hidden` root (`app-shell.tsx`). `Topbar` (`app-shell/topbar.tsx`) dropped its `collapsed` prop and the brand (now in the sidebar); three balanced zones — hamburger (`< lg`) / centered capped search (`max-w-110`) / right cluster. (2) **Topbar controls** are now bordered icon buttons (theme + notifications via `variant="outline"`), the bell has a `bg-error` notification dot, and a hairline divider precedes the user menu. (3) **Table rows** use the handoff's **24px / 15px** cell padding (`px-6 py-3.75`) in the shared `Table` primitive. (4) **Row action buttons** (admissions Reject/Approve) and the **pager buttons** are the handoff's **34px** height (`h-8.5`, `text-[13px]`). (5) **Pagination** moved **inside the table card as a bordered footer** (`border-t … px-6 py-3.5`) per the design, with a separate pager under the mobile card list (all three lists). `tsc` + `eslint` clean.
- **Teacher (2.4) + student (2.7) detail pages adopted the same "Admission Detail" design:** the hero/cards primitives were extracted into a shared kit `components/detail/detail-ui.tsx` (`DetailLayout`, `DetailBackLink`, `DetailHero`, `DetailCard`/`DetailCardHeader`, `DetailRow`, `DetailSubHeading`, `heroActionClass`) — a `TONE_ACCENT` map keys the hero's pill + avatar ring + top accent bar off a `StatusTone` (`success`/`warning`/`error` use the design's exact pastels with dark variants; `info`/`neutral` extend the same treatment). `teacher-detail.tsx` and `student-detail.tsx` were rebuilt on it: status-accented hero (avatar/initials, name + subtitle, status pill, 40px `heroActionClass` actions, a 4-up key-facts strip) over icon-headed `DetailCard`s with split `DetailRow`s. **Teacher** tone = active→success / inactive→error; facts = Employee ID · Designation · Phone · Joined; cards Profile (`User`) + Assignments (`BookOpen`, chips right-aligned). **Student** tone = active→success / inactive→error / **tc→info**; the TC info banner is kept above the hero; facts = Admission no · DOB · Religion · Admitted; cards Student (`User`) / Guardians (`Users`) / Present + Permanent address (`MapPin`), then Enrollment history wrapped in a `DetailCard` (`GraduationCap`) around the unchanged `StudentEnrollments` table. Data layer, dialogs, permission gates, and not-found/error/loading states unchanged. The admission detail keeps its own copy of the primitives (already shipped/approved). The hero **title is single-line** (`truncate`), and actions render through a shared `DetailActions` overflow cluster — the `primary` action (Edit) stays inline as the filled button while every other action (Attendance/Results/ID card/TC/Photo/Resend/Activate-Deactivate) collapses into a `⋮` dropdown (`router.push` for nav items, `onSelect` for dialogs), so the hero stays on one row no matter how many actions a page has. `tsc` + `eslint` clean.
- **Admission detail (2.6) redesigned to the "Admission Detail" Claude Design handoff:** `components/admissions/review/admission-detail.tsx` rebuilt to the imported design (connector connected, design-system scopes granted) — recreated with theme tokens so light/dark + accent themes still work, while the data layer (`useAdmission`, approve/reject dialogs, `admissions.manage` gate, not-found/error/loading states) is unchanged. A **status-accented hero** card (top accent **bar** + avatar **ring** + soft **status pill**, all keyed off pending/approved/rejected via a local `STATUS_ACCENT` map carrying the design's exact pastel hexes **with dark-mode variants** — the only literal hex, since the three lifecycle accents aren't in the token set): avatar (applicant photo or initials), name + name_bn, the status pill, the Reject (outline, red-on-hover)/Approve (primary) actions, and a 4-up **key-facts strip** (Application no · Desired class · DOB · Submitted). Below, a 2-col grid of **icon-headed info cards** (Student / Admission / Guardian / Address) with `label … value` rows split by hairlines; Guardian groups Father/Mother under uppercase sub-headings and merges en · bn names; Address shows a green **"Current"** badge on Present and a **"Same as present"** check badge on Permanent when the joined strings match. Then a **Previous education** card (each row: exam-abbreviation tile + name + `institution · Passed {year}` + a `GPA n.nn` brand chip) and a **Documents** card (dashed-bordered empty state with an `ImageOff` medallion when none; the existing download-link grid when present). The non-functional "Reopen" control from the mock was dropped (no backend reset route); the app's rejection-reason panel was kept. `tsc` + `eslint` clean.
- **Teacher profile assignments (2.4) — nested-shape fix:** the same nested-relation expansion affected the teachers module. `TeacherAssignmentSummary` (`types/teacher.ts`) now carries optional nested `class`/`school_class`/`section`/`subject` objects (and `class_id` is optional), with an `assignmentSummaryLabels()` resolver mirroring `assignmentLabels`. Applied to: the detail page assignments cards (`teacher-detail.tsx`, was showing "Class #undefined"), the edit form's assignment pre-fill (`teacher-form-dialog.tsx`, resolves `class_id`/`section_id`/`subject_id` from flat or nested), and the list/detail subject/class chips (`teacher-summary.ts`). Also `useTeacherOptions` now normalises `/teachers` to a bare array whether the backend returns a flat array or a nested paginator, and `TeacherSelect` coerces option ids to numbers (fixes the assignment teacher filter + edit-dialog teacher field).
- **Teacher detail (2.4) — tab strip + inline-editable cards (aligned to student detail 2.7):** `teacher-detail.tsx` now mirrors the student detail layout. Below the hero sits a URL-persisted `DetailTabs` strip (`useDetailTab`, `?tab=`) with **Profile** (default) and **Attendance** (gated `attendance.view`); the tab body is wrapped in a `Suspense` boundary like the student screen. The hero **Edit button was removed** — the `TeacherFormDialog` is no longer reachable from the detail page (create/edit still live on the list); the kebab keeps Change photo / Resend credentials / Activate-Deactivate. The Attendance hero action moved into the Attendance tab (an `EmptyState` with an "Open attendance" link to `/attendance?teacher={id}`). New `components/teachers/teacher-profile-cards.tsx` (modelled on `student-profile-cards.tsx`) exports `TeacherProfileCard` + `TeacherAssignmentsCard`: each `DetailCard` flips between read-only `DetailRow`s and an inline edit form via a header `Pencil` ghost-icon button (same icon/size/styling as the student cards), gated on `teachers.manage`. Saving sends the full `PUT /teachers/{id}` payload (a shared `teacherToPayload(teacher)` base merged with the card's edits) so editing one card never drops the other's values; `422` → field errors + `FormBanner`, success → toast + back to read view. Profile edits name/phone/employee_id/designation/gender(`Select`)/address (email + joining date are immutable/out-of-contract → static rows); Assignments reuses `TeacherAssignmentsField` with the same "class required per row" guard as the edit dialog. `tsc` + `eslint` clean.
- **Design system synced to the refreshed `docs/ui-context.md` (default theme → light + Purple):** the `docs/ui-context.md` was rewritten into a full App-wide design system grounded in the Mark Entry screens, changing the shipped default accent from Indigo to **Purple** and the canonical accent set to six (Purple/Blue/Teal/Rose/Amber/Cyan — Indigo dropped). Brought the implementation in line: (1) `packages/ui/src/styles/globals.css` — neutral palette re-pointed to §1.2 values for both modes (page `#f7f7f8`, subtle `#fafafa`, hover `#f4f4f5`, track `#efeff1`, structural vs default/strong borders, row-divider, text faint), new tokens `--bg-hover`/`--bg-track`/`--row-divider`/`--text-faint`/`--accent-soft-border` exposed as `color-hover`/`color-track`/`color-divider`/`color-copy-faint`/`color-accent-soft-border` utilities; `:root` + `.dark` accent baselines set to Purple; status tokens updated to §1.5 fg values (success `#15803d`, warning `#c2410c`, info `#2563eb`); per-accent blocks now carry explicit light soft/soft-border tints (§1.4) with 14%/28% alpha derivatives on dark; Indigo block removed and emerald re-pointed to the spec Teal `#0d9488`. (2) `accent-provider.tsx` — `ACCENTS` reordered to the six-accent set (Emerald labelled "Teal"), `DEFAULT_ACCENT = "purple"`, stale "Blue is the baseline" comment fixed. (3) `app/layout.tsx` pre-hydration accent script guards on `"purple"`. (4) Two 76px entity-avatar fallbacks (`detail-ui.tsx`, `admission-detail.tsx`) switched from raw `indigo-*` to `violet-*` to match the Purple brand gradient. The bespoke public-admission shell keeps its dedicated scoped palette. `turbo typecheck` passes.
- **Shared UI primitives aligned to `ui-context.md` §3 (full design-system pass):** reconciled every `packages/ui/src/components` primitive to the spec measured values so the system propagates app-wide. **Button** (§3.1): default h32→h38 (`h-9.5`), lg→h42 (`h-10.5`), weight 500→600, primary now carries the accent shadow `0 2px 8px rgba(accent,.28)` via `color-mix`; icon sizes 32/36→38/42. **Input/Textarea** (§3.3): h44→h40 (`h-10`), radius 14→10 (`rounded-lg`), text 16→14 (`text-sm`). **Select** trigger (§3.4): h44→h40, radius→10, text→14, padding `px-3.5`. **Badge** (§3.6): `rounded-4xl`→`rounded-full`, weight 500→600, padding→`px-2.5 py-1`, gap→1.5 for the status dot. **Card** (§3.9): `ring-1`→structural border + card shadow `0 1px 3px rgba(16,16,20,.05)`, default padding 16→20, title 16/500→17/600. **Table** (§3.11): header fill opaque subtle-surface, cell padding 24/15→18/9 (`px-4.5 py-2.25`), header `px-4.5 py-3.25`, header text 12→11.5px. **Dialog** (§3.15): radius 18→16, added modal shadow `0 24px 60px rgba(16,16,20,.22)`, max-width sm→xl (~576px). **Popover/Dropdown**: shadow→spec popover `0 8px 24px rgba(16,16,20,.12)`. **Sheet**/**Label**/**Card** titles→600. **Avatar** lg 40→42. **Sonner** radius→12px. **Status tokens** (§1.5): added `--state-{success,warning,error,info}-{soft,border,dot}` for light+dark and exposed as utilities; `StatusBadge` now uses exact soft/border/dot values. `turbo typecheck` + `next build` (23 routes) pass. Not changed (intentional): the bespoke hero status-accent maps and the public-admission scoped palette.
- **Academic section aligned to the refreshed design system (post-2.2/2.3 design-system sync):** the `/academic` screens had drifted from the updated `ui-context.md` / shared primitives. Three concrete mismatches fixed without touching data/permission logic: (1) **Tabs** — `academic-management.tsx` was rendering the bare default `TabsList`/`TabsTrigger` (compact `h-8` `bg-muted` neutral pill), now uses the app-wide segmented-control convention shared with the Attendance hub (subtle `rounded-xl bg-subtle p-1` track + `h-9` triggers that fill to `bg-surface` with `text-copy-primary` + shadow when active), via a shared `tabTriggerClass`. (2) **Select** — the native `<select>` in `academic-select.tsx` (behind every `Session/Class/Section/Subject/Teacher/Branch Select`) used legacy values (`rounded-[10px]`, `text-[15px]`, `border-surface-border`, `bg-surface`, `pl-3`), realigned to the §3.3/§3.4 input tokens (`rounded-lg`, `text-sm`, `border-input`, `bg-transparent`/`dark:bg-input/30`, `px-3.5`, `disabled:bg-input/50`, `text-muted-foreground` placeholder + spinner) so it matches the shared `Input`/`Select` primitives exactly. (3) **Checkbox** — `session-form-dialog.tsx`'s "current session" checkbox used `accent-[--accent-primary]`; switched to the app's `accent-brand` convention used by every other form checkbox (students/admissions). Data tables, buttons, form inputs, dialogs, and row actions already consumed the shared primitives and needed no change. `turbo typecheck` passes.
- **Academic → Classes tab visual polish:** the classes accordion (`classes-manager.tsx` `ClassRow` + `class-children.tsx`) was reading flat/plain. Redesigned to the design-system card aesthetic without touching the data/permission/expand logic: each class row now leads with a `size-10` accent medallion (`bg-accent-dim` + `BookOpen` in `text-brand`), a `font-semibold` name over a muted `Level N · Sections & subjects` sub-line, and a right-aligned rotating chevron; the row card gains `shadow-sm` and, when expanded, an `accent-soft-border` outline + faint `bg-accent-dim/50` header tint. The expanded panel wraps **Sections** and **Subjects** as their own surface sub-cards (`border bg-surface`), each header now icon-led (`Layers`/`Library`) with a mono count chip and the Add button; rows render as a clean divided mini-list (`divide-y` inside a single rounded border, hover `bg-subtle`, a small accent dot leader, meta as a mono chip) instead of individually-bordered pills, and the empty state is a centered dashed `bg-subtle/40` panel. `tsc` + `eslint` clean.
- **Student result sub-tabs (2026-07-05):** the Results tab on the student detail page (and the student/parent "my results" view in `profile-view.tsx` / the results page) now opens behind a semester sub-tab strip — **First Semester / Second Semester / Annual Exam Result** — instead of dumping the whole bundle. `result-bundle-panel.tsx` gained a local `TabbedResultBundle` (state-only, defaults to First Semester) styled like the results page's `StaffViewTabs`; the two semester tabs pass their `ExamType` through the existing `examType` narrowing, and the Annual tab shows the `final` exam card together with the cross-semester `AnnualResultCard` via a new `showAnnual` override prop on `ResultBundlePanel` (default behaviour for existing callers unchanged). `tsc` clean.
